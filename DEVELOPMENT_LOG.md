# 开发日志

本文件记录“和工作”的每次项目更新。时间均使用中国标准时间（Asia/Shanghai）。

## 2026-08-14 17:11:47 +0800

- 将 GitHub 仓库 `Aoppp/DSH_HeGongZuo` 克隆到本地项目目录，并确认 `main` 分支跟踪 `origin/main`。
- 查询并确认 DSH 当前发布版本，选择精确锁定 `@deepseek-ai/dsh@0.1.0-rc.6`，避免预发布阶段的非预期升级。
- 锁定运行环境为 Node.js `>=22.19.0`、pnpm `11.7.0`，建立根 `package.json` 与 pnpm workspace。
- 添加 DSH CLI、DSH Web、配置查看和环境验证命令。
- 添加 `.env.example` 和 `.gitignore`，确保真实 API 密钥与本地运行文件不会进入 Git。
- 添加环境检查脚本 `scripts/verify-environment.mjs`。
- 编写 README 和 DSH 技术路线文档，明确只先开发员工管理 Agent，并采用“Mock 成熟后替换正式数据层”的路线。
- 直接使用临时 `npx` 获取 DSH 时出现长时间无输出，已停止该进程，改用项目内精确版本依赖，保证安装和调用方式可复现。
- pnpm 首次安装触发依赖构建脚本保护；已将 DSH 依赖链中需要安装脚本的 5 个精确包显式加入 `allowBuilds`，不采用全局或通配放行。
- 建立严格的 TypeScript 根配置，锁定 TypeScript `7.0.2` 与 Node.js 22 类型定义，并增加 `build`、`typecheck` 命令，为后续 workspace 业务包提供统一基线。

## 2026-08-14 17:17:01 +0800

- 完成依赖安装并生成 `pnpm-lock.yaml`；pnpm 供应链策略检查通过，DSH 所需的原生终端与子进程依赖构建成功。
- 执行 `build` 和 `typecheck`，严格 TypeScript 检查全部通过。
- 执行环境验证，确认本机 Node.js `22.22.2`、项目 DSH `0.1.0-rc.6`、pnpm `11.7.0` 均符合锁定要求。
- 验证 DSH CLI 帮助和 web profile 配置可以正常加载。
- 实际启动 DSH Web，并确认 `http://127.0.0.1:3080/` 返回 HTTP 200；验证完成后已正常停止测试进程。
- 检查项目文件，未发现真实 DeepSeek API Key；模型请求暂不测试，待开发者在本地 `.env` 中配置密钥后进行。

## 2026-08-14 17:30:00 +0800

- 完成 Phase 1 员工管理 Agent 产品与技术规格，定义首批场景、用户角色、权限矩阵、工具契约、统一响应结构、变更确认流程和 Phase 2 验收标准。
- 新建 workspace 包 `@hegongzuo/employee-domain`，提供员工状态、用工类型、员工记录和数据集的 TypeScript 类型。
- 新建员工数据集 JSON Schema，约束字段、枚举、ID、工号、经理关系和数据来源标记。
- 创建 10 名完全虚构的员工测试数据，覆盖 6 个部门、在职/试用/休假状态和多级汇报关系；邮箱使用 `example.com`，电话使用明显无效的测试号码。
- 增加 `validate:data` 命令和数据校验脚本，检查记录数量、必填字段、唯一性、日期、枚举、经理引用和循环汇报。
- 更新根 `build`、`typecheck` 命令，使其同时检查所有 `packages/*` workspace 包。
- 首次严格类型检查发现校验脚本存在 3 处动态 JSON 类型边界问题，已增加明确类型声明；随后发现脚本子进程无法直接解析全局 `pnpm`，已统一改为通过 Corepack 调用。
- 重新执行数据校验、构建和类型检查，全部通过。
- 更新 README 和技术路线，将当前进度调整为 Phase 0、Phase 1 已完成，下一步进入 Phase 2 的 DSH 插件接入。

## 2026-08-14 17:39:34 +0800

- 核对本地 DSH `0.1.0-rc.6` 的插件、工具注册、输出 Schema、系统提示和 profile bundle API，确认使用 Cordis bundle 扩展，不修改 DSH 内核。
- 新建 `@hegongzuo/employee-agent` workspace 包，声明 `dsh.bundle` 和 `cordis.patch.yml`，使插件能通过 DSH 官方 profile 机制加载。
- 实现只读员工仓储，支持关键词、部门、岗位、状态、地点和分页查询，以及按 ID/工号查询与部门成员查询。
- 注册 `employee_search`、`employee_get`、`organization_list_members` 三个模型兼容的只读 DSH 工具，并为每个工具声明输入、输出 Schema 和并发安全属性。
- 增加员工管理系统提示，要求 Agent 明确说明当前数据为虚构测试数据，并禁止声称执行了入职、调岗、离职或员工信息修改。
- 插件构建时从唯一的员工领域 fixture 复制运行数据，避免手工维护两份测试数据；运行时只接受带有 `synthetic-non-personal` 标记的数据集。
- 增加 6 项自动化测试，覆盖数据加载、技术部查询、工号查询、经理解析、空部门结果、工具定义和 DSH 插件入口注册。
- 首次类型检查发现插件缺少 Node 类型和可选参数边界问题，已在保持严格类型规则的前提下修复，未关闭 `exactOptionalPropertyTypes`。
- 通过 Corepack 启用 pnpm shim，并将本地员工插件安装到 `~/.dsh/profiles/web`；profile manifest 已包含 `@hegongzuo/employee-agent` bundle。
- 因用户正在运行的 DSH 占用 3080 端口，保留该进程不做中断；改在 3081 启动独立验证实例，DSH Web 成功加载插件并返回 HTTP 200，验证后已停止 3081 实例。
- 更新 README、员工 Agent 规格和技术路线，补充插件安装、运行时工具名、网页测试方式及当前 Phase 2 进度。
- 最终执行测试数据校验、构建、类型检查和 6 项自动化测试，全部通过。

## 2026-08-17 10:31:35 +0800

- 根据新的产品优先级，暂停扩展 Agent 功能，开始交付“和工作”线上办公协作平台的第一版静态视觉原型。
- 新建 `apps/web` React 应用，使用成熟企业科技蓝设计，完成品牌入口页、侧边导航、顶部工具栏、响应式布局和通用视觉系统。
- 建立面向全体员工的分角色工作台，可预览普通员工、团队负责人、HR 管理员、管理层和超级管理员五种界面状态。
- 将系统管理员定义为企业所有者和平台最高权限角色，增加仅超级管理员可见的企业管理入口与静态管理页。
- 完成工作台、消息、项目、任务、文档、日程、员工、Agent 中心和企业管理的页面样式；除员工管理 Agent 外，这些模块仅作为静态界面预览。
- 将平台侧 Agent 入口品牌定为“和助手”，增加全局助手抽屉、员工页入口和 Agent 中心；实际员工查询通过链接进入已安装 bundle 的 DSH Web。
- 增加 `platform:dev` 和 `platform:preview` 命令，并将 `apps/*` 纳入 pnpm workspace、根构建和类型检查流程。
- 新增平台环境变量示例 `VITE_DSH_WEB_URL`，默认指向本地 DSH Web 的 `http://127.0.0.1:3080`。
- 处理 DSH `0.1.0-rc.6` 依赖链的 React 主版本分叉，在 workspace 将 React 和 React DOM 统一锁定为兼容的 `18.3.1`，`pnpm peers check` 无遗留依赖问题。
- 更新 README 和技术路线，新增平台视觉原型说明，明确已交付界面、Agent 连接方式和未实现功能的边界。
- 启动平台生产构建预览并验证首页、JavaScript 和 CSS 均返回 HTTP 200；最终环境检查、10 名虚构员工数据校验、严格类型检查、平台生产构建、6 项 Agent 测试、peer dependency 检查和文件格式检查全部通过。

## 2026-08-17 11:04:41 +0800

- 根据“一步一步开发”的新要求大幅收缩平台范围，删除消息、项目、任务、文档、日历、员工列表和企业管理等未开发模块的页面与导航。
- 删除原来集中在单个 `App.tsx` 中的页面与角色逻辑，建立 `app`、`components`、`config`、`modules` 和 `styles` 分层目录。
- 新建唯一模块注册表 `module-registry.ts`，统一管理功能 ID、导航名称、描述、图标、组件和允许角色；导航和访问过滤不再重复维护。
- 将概览、员工管理 Agent 和开发控制台拆分为三个独立功能目录，每个模块拥有自己的 React 组件与 CSS，可独立增加、删除或修改。
- 将原来的全局角色模型精简为“老板”和“开发者”；老板代表企业所有者和最高业务权限，开发控制台仅开发者可见。
- 保留前端身份切换作为内部评审工具，并在界面与文档中明确说明它不是正式账号认证；HR、管理者和普通员工尚未加入当前代码。
- 重写员工管理 Agent 模块，将 DSH Web 直接嵌入“和工作”内容区，删除跳转到外部页面的入口，增加内嵌页重新加载与平台内专注模式。
- 将原来集中的 3,063 行平台 CSS 替换为设计变量、全局基础、平台外壳和三个模块局部样式，降低后续修改的相互影响。
- 更新 README、技术路线和平台原型说明，补充模块的增加、删除、修改方式以及 DSH 内嵌运行结构。
- 验证当前运行中的和工作开发服务与 DSH Web 均返回 HTTP 200，DSH 未返回禁止内嵌的响应头；全项目环境校验、数据校验、类型检查、生产构建、6 项 Agent 测试、peer dependency 检查和格式检查全部通过。

## 2026-08-17 11:13:06 +0800

- 删除登录页的身份选择和登录后的角色切换，改为标准账号、密码登录表单。
- 新建独立账号边界 `accounts.ts`，由账号记录自动返回老板或开发者角色，并将当前用户姓名、账号和角色传入平台权限过滤。
- 新增 `boss / demo123` 和 `developer / demo123` 两个本地测试账号；老板登录后默认进入管理驾驶舱，开发者默认进入概览。
- 在代码与界面中标记当前登录仅用于本地开发，正式上线前必须将前端账号校验替换为服务端认证与 API 权限校验。
- 新增仅老板可见的“管理驾驶舱”模块，并通过统一模块注册表接入导航和角色访问过滤。
- 完成员工、项目、销售、财务和审批五类核心指标卡，以及销售/回款趋势图、分级异常提醒、子系统健康度和 AI 经营分析区域。
- 将驾驶舱所有指标、趋势数组、异常、系统状态和 AI 建议集中到独立 `cockpit-data.ts`，与页面布局和局部样式分离。
- 当前驾驶舱数据均为测试用虚构经营数据，AI 分析为 Mock 文本，未连接真实 HR、项目、CRM、财务或审批系统，也未调用模型。
- 为平台新增 4 项账号测试，覆盖老板角色映射、开发者角色映射、账号归一化和错误密码拒绝。
- 更新 README、平台模块化说明和技术路线，新增管理驾驶舱专项文档，记录数据边界和正式子系统接入顺序。
- 最终检查确认前端代码中已无身份选择器和角色切换器，运行中的平台和 DSH 均返回 HTTP 200；环境校验、数据校验、严格类型检查、生产构建、4 项账号测试、6 项 Agent 测试和依赖检查全部通过。

## 2026-08-17 11:27:34 +0800

- 定位员工管理 Agent 在不同平台账号间共享会话记录的原因：老板和开发者同时嵌入默认 `3080` DSH Web，共享同一浏览器 origin 与默认 `~/.dsh/sessions` 服务端会话目录。
- 将平台模块属性从仅接收角色扩展为接收完整登录用户，员工 Agent 现在使用账号 ID 解析专属运行时。
- 新增统一账号 Agent 运行时定义 `config/account-agent-runtimes.json`：`boss` 分配 3180 端口，`developer` 分配 3181 端口。
- 为两个账号创建 `.runtime/dsh/boss` 和 `.runtime/dsh/developer` 两个独立 DSH_HOME，并分别初始化 web profile、安装 `@hegongzuo/employee-agent` bundle。
- 新增 `dsh:accounts:setup` 命令用于幂等配置全部账号运行空间，新增 `dsh:accounts` 命令用于同时启动所有专属 DSH Web。
- 员工 Agent iframe 改为按登录账号使用独立 URL，iframe key 包含账号 ID；登出时组件完全卸载，下一账号不会复用前一账号的 iframe。
- 删除共享 DSH URL fallback；任何未配置专属运行时的账号都会看到隔离保护提示，不会被自动导向共享会话。
- 新增 3 项隔离回归测试，检查账号的浏览器 origin 不同、账号/端口唯一以及未配置账号无共享 fallback。
- 新增 `verify:agent-isolation` 服务端验证命令，已确认两个 DSH_HOME 独立、两个端口独立、插件已分别安装，且 3180 和 3181 均返回 HTTP 200。
- 原默认 `~/.dsh` 中的共享记录未删除，但平台不再访问 3080；将 `.runtime/` 加入 Git 忽略，避免提交本地账号会话和配置。
- 更新 README、平台说明和环境变量模板，新增 Agent 账号隔离专项文档。
- 首次全量测试发现 Node.js 直接运行 TypeScript 测试时要求 JSON import attribute，已增加 `with { type: 'json' }` 并使用显式 `.ts` 扩展名，同时保持 Vite 构建与 Node.js 测试兼容。
- 最终环境校验、测试数据校验、严格类型检查、生产构建、7 项账号/隔离测试、6 项员工 Agent 测试、peer dependency 检查与运行时隔离检查全部通过。

## 2026-08-17 11:50:54 +0800

- 删除员工管理 Agent 模块中的 DSH Web iframe，不再向老板或开发者展示 DeepSeek/DSH Logo、导航、工作区选择和产品声明。
- 新建“和工作”原生员工 Agent 操作界面，包含最近会话、新建对话、历史消息、推荐问题、消息输入、连接状态、运行状态、工具调用状态和错误提示。
- 新建账号 DSH API 客户端，通过平台同源代理调用 DSH 的 Workspace、Session、History 和 Prompt HTTP API，并使用 WebSocket 接收会话与运行状态事件。
- Vite 开发与预览服务新增按账号划分的 HTTP/WebSocket 代理：`/dsh/boss` 转发到 3180，`/dsh/developer` 转发到 3181；浏览器不再直接加载 DSH Web origin。
- 新建 DSH 原始事件到“和工作”消息模型的转换层，支持用户消息、流式助手文本、最终回答、工具调用/结果以及轮次错误的原生展示。
- 将账号运行时定义扩展为同时声明唯一 API 路径和唯一 Workspace；老板与开发者分别使用 `.runtime/workspaces/boss/employee-agent` 和 `.runtime/workspaces/developer/employee-agent`。
- 更新账号运行时 setup/start 脚本：自动创建 Workspace，以该目录作为 DSH 进程 cwd，并通过环境变量将账号工作区传入员工 Agent 插件。
- 员工 Agent 插件增加 DSH Workspace Registry 依赖，在运行时启动时自动注册后台分配的工作区；用户不再输入或选择本地文件路径。
- 扩展隔离验证，新增 DSH_HOME、端口、API 路径、Workspace 唯一性和 Workspace 实际注册检查；两个账号的服务与工作区均验证通过。
- 新增 2 项对话事件转换测试，并扩展账号配置和员工插件测试；平台共 9 项测试、员工 Agent 共 6 项测试全部通过。
- 新增 `THIRD_PARTY_NOTICES.md`，保留 DSH 的 MIT License 归属和发布义务，同时明确第三方技术名称不作为“和工作”的用户界面品牌。
- 更新 README、平台说明、账号隔离说明和技术路线，记录原生 UI、自动 Workspace、平台代理及正式部署的认证边界。
- 重新安装并启动两个账号专属 DSH 运行时和“和工作”开发服务；通过平台代理验证 Workspace API、Session API、History API 与 WebSocket `session/subscribed` 事件均正常工作。

## 2026-08-17 13:11:05 +0800

- 修复浏览器调用员工 Agent 时 `transport failure for /api/workspace.list: HTTP 403`：根因是浏览器的平台代理 Origin 被原样转发，未通过 DSH 运行时的同源安全校验。
- 为每个账号的 Vite HTTP/WebSocket 代理显式重写 Origin，使代理到 3180/3181 的请求使用对应 DSH 运行时 Origin，同时保留浏览器只访问“和工作”同源路径的架构。
- 使用携带真实浏览器 Origin 的 Workspace API 请求验证修复，返回 `ok: true`；使用 WebSocket Upgrade 请求验证 `/dsh/boss/api/events.mux` 返回 HTTP 101。

## 2026-08-17 13:17:39 +0800

- 新增根目录 `AGENTS.md` 项目执行约束，明确只执行用户当前命令要求及不可缺少的最小操作，禁止自行扩展为构建、测试、验证、重启、部署、重构或其他修改。
- 明确命令存在歧义或确需扩大范围时，必须先停止并取得用户许可。
- 保留开发日志更新要求，并明确任何日志、文档和输出均不得记录或回显 API Key、密码等秘密值。

## 2026-08-17 13:25:41 +0800

- 在员工管理 Agent 标题区新增“维护员工数据”按钮，点击后进入“全体员工信息”页面，可查看当前全部员工档案。
- 在全体员工页面新增“进入维护”按钮，进入后提供员工搜索、新增、详情查看、编辑和删除操作。
- 新增员工编辑侧栏，覆盖编号、姓名、邮箱、电话、部门、岗位、直属经理、用工类型、状态、入职日期和工作地点字段，并校验必填项、员工编号及邮箱唯一性。
- 删除员工时同步清空其下属的直属经理引用，避免保留无效汇报关系。
- 当前维护结果保存于浏览器本地存储，并在界面标注为“本地测试数据”；未新增后端数据库或修改 DSH Agent 的只读数据源。

## 2026-08-17 13:28:02 +0800

- 将员工数据维护从 Agent 内部入口扩展为平台独立模块，在侧边导航新增“员工数据”，老板和开发者均可直接进入。
- 新增轻量模块适配组件，通过统一模块注册表接入现有导航与权限过滤逻辑。
- 侧边导航入口与员工管理 Agent 内入口复用同一个员工列表、维护界面和浏览器本地数据源，没有复制业务逻辑或产生两份员工数据。

## 2026-08-17 13:55:42 +0800

- 新增 `apps/api` TypeScript 服务，使用 PostgreSQL 提供员工列表、员工详情、新增、更新和删除 REST API。
- 新增 PostgreSQL 员工表迁移，包含员工编号与邮箱唯一约束、用工类型与状态约束、直属经理外键、自我汇报限制及查询索引。
- 新增数据库迁移与测试数据初始化命令；初始化操作幂等保留已有员工，不覆盖数据库中已经修改的记录。
- 将员工数据维护界面从浏览器 `localStorage` 切换为真实员工 API；数据库不可用时明确显示错误，不再回退到本地假数据。
- 新增前端 `/api/employees` 同源代理，并调整 `platform:dev`，使平台开发命令同时启动 Web 与员工 API 服务。
- 在 Git 忽略的 `.runtime/postgres` 初始化并启动本地 PostgreSQL，仅监听 `127.0.0.1:5432`；连接配置写入 Git 忽略的 `.env`，未记录任何秘密值。
- 完成员工表迁移并写入 10 条虚拟员工测试数据；真实新增、读取、更新、删除检查全部通过，临时验证记录已删除，最终数据库保持 10 条记录。
- 通过“和工作”开发代理读取员工 API 成功，返回 HTTP 200 和 10 条数据库员工记录。
- DSH 员工管理 Agent 本轮未改动，仍使用其现有只读数据源；本次真实 CRUD 范围为“和工作”员工数据维护模块。

## 2026-08-17 14:09:06 +0800

- 按新的员工数据要求，从共享员工领域模型、测试数据、DSH 查询结构、PostgreSQL API 和平台维护界面中删除“员工编号”和“部门编码”。
- 新增 PostgreSQL `002` 迁移并执行，真实删除 `employees.employee_no` 与 `employees.department_code` 两列，保留其余 11 条员工记录。
- 将 `work_email` 从必填字段改为可空字段；数据库解除非空约束，API 接受 `null` 或空值，维护表单改为“工作邮箱（选填）”。
- 更新数据库初始化脚本与员工测试数据 Schema，确保全新数据库也直接使用新的字段结构。
- 更新员工 Agent 的现有只读工具结构：关键词不再匹配员工编号或部门编码，员工详情仅按员工 ID 查询，部门成员仅按部门名称查询。
- 使用空邮箱完成真实新增与删除验证，接口响应确认不再包含已删除字段；临时记录已清除，数据库最终保持 11 条员工数据。

## 2026-08-17 14:25:38 +0800

- 将员工预览“小眼睛”按钮从“进入维护”模式移至全体员工信息主界面；维护模式仅保留编辑和删除操作。
- 小眼睛改为打开员工简历侧栏，集中显示员工职责、简历文件信息和简历内容；PDF 可在平台侧栏内预览，DOC/DOCX 提供查看或下载入口。
- 新增员工与编辑员工表单增加“员工职责”多行文本框，以及 PDF、DOC、DOCX 简历上传控件，单文件限制为 5 MB。
- 新增 PostgreSQL `003` 迁移，增加职责、简历文件名、简历 MIME 类型和简历二进制列；简历文件真实保存为 PostgreSQL `bytea` 数据。
- 新增员工简历读取接口，返回原始文件内容及正确的内容类型、文件名和禁止缓存响应头。
- 扩展共享员工领域模型、测试数据 Schema、测试 fixture 和员工 Agent 输出结构，使其包含职责与简历元数据，但不向 Agent 返回简历二进制内容。
- 使用临时 PDF 完成真实职责写入、简历上传、元数据读取和文件内容读取检查，结果全部通过；临时员工已删除，数据库保持 11 条员工记录。

## 2026-08-17 14:30:45 +0800

- 修复员工简历预览侧栏中“编辑员工”按钮点击后缺少明确界面切换的问题。
- 点击“编辑员工”现在会进入员工数据维护模式，重新载入当前员工草稿，并强制将简历预览侧栏切换为可编辑表单。
- 开发服务热更新完成且未报告该组件编译错误。
- 只读检查确认 PostgreSQL 当前有 1 份真实简历二进制数据，存储大小为 1,015,354 字节；查询未读取或输出简历内容。

## 2026-08-17 14:41:14 +0800

- 修复员工维护页面新增员工后，员工管理 Agent 仍固定返回 10 名测试员工的数据不同步问题。
- 新增 Agent 专用 PostgreSQL 只读仓储；每次 `employee_search`、`employee_get` 或 `organization_list_members` 工具调用都会重新读取当前员工表快照，不缓存固定 JSON。
- 将员工工具抽象为统一只读数据源接口，保留内存仓储用于测试，同时让正式 DSH 插件使用 PostgreSQL 仓储。
- 更新 Agent 系统提示与工具描述，移除“固定虚构测试数据”声明，明确员工数量和名单必须先调用工具获取最新数据库数据，且工具仍禁止写入、修改或删除。
- 为 Agent 插件增加 PostgreSQL 依赖、连接启动检查与 Cordis 生命周期连接池清理；老板和开发者运行时共享公司员工数据，但继续保持各自会话和工作区隔离。
- 员工领域、平台、API 和 Agent 全量构建成功；重新安装插件并重启 3180/3181 两个账号专属 DSH 运行时。
- 直接执行新版 `employee_search` 返回 11 条记录；进一步通过老板 DSH 运行时发起真实 Agent 提问，确认调用了 `employee_search` 且回答包含当前 11 名员工。

## 2026-08-17 15:09:02 +0800

- 在员工管理 Agent 的“最近对话”列表中为每条对话增加独立删除按钮，支持鼠标悬停、键盘聚焦、删除中状态和正在处理时禁用。
- 删除前显示不可恢复确认；删除当前对话后自动切换到剩余的最新对话，无剩余对话时回到空白欢迎界面。
- 员工 Agent DSH 插件新增账号运行时内的会话永久删除路由，精确校验会话 ID、所属工作区和 JSONL 存储路径，禁止跨账号或跨工作区删除。
- 实现 DSH 空闲 Agent 句柄跟踪：运行中的对话拒绝删除，空闲对话先通过 DSH 原生句柄有序停止、刷新并卸载，再执行持久化清理，避免会话文件与内存状态竞态。
- 后台删除同步清理工作区会话引用、会话标题等投影缓存，以及完整会话日志目录；不再使用仅隐藏文件的 DSH 归档接口。
- 在开发者账号中新建并删除一条专用空白临时对话，确认删除后会话目录、工作区引用、投影缓存和会话列表均不再包含该会话。

## 2026-08-17 15:16:33 +0800

- 将“后续开发必须模块化”写入根目录 `AGENTS.md` 项目执行约束，作为持续适用规则。
- 明确平台功能的独立目录和统一模块注册要求，禁止在导航、入口和权限中重复硬编码。
- 明确界面、状态、数据访问、业务规则、Agent 能力和第三方框架适配的职责边界，要求通过接口组合。
- 明确跨模块共享边界、禁止依赖其他模块内部实现，并要求模块增删替换不得破坏其他功能。

## 2026-08-17 15:35:58 +0800

- 修复 `platform:dev` 只启动 API 与 Web、未启动账号 DSH Agent 运行时的架构缺口，该缺口会导致平台代理 `/api/workspace.list` 时无法连接 3180/3181 而返回 HTTP 502。
- 更新 `scripts/run-platform-development.mjs`：平台启动时自动执行账号 Agent 初始化、启动两个专属 DSH 运行时，并等待 boss、developer 的 3180/3181 端口可访问后才启动平台 API 与 Web。
- 新启动器将 Agent 运行时作为平台子进程统一管理；任一 Agent 运行时退出时，平台启动器会同步停止 API 和 Web，避免留下持续返回 502 的孤立平台页面。
- 完成启动验证：4173 平台、3180 boss Agent、3181 developer Agent 均返回 HTTP 200；通过平台代理调用 boss `workspace.list` 返回成功。

## 2026-08-17 15:46:22 +0800

- 处理重复执行 `platform:dev` 时 4173、4174 端口被此前平台实例占用而导致 Vite 和 API 启动失败的问题。
- 更新平台启动器，在构建和启动任何子服务前预检 4173、4174、3180、3181 端口；如有占用，直接指出端口及“先在旧终端按 Ctrl+C 停止”的处理方式，不再进入 Vite/API 的异常堆栈。
- 停止本次诊断前早先启动的旧平台实例，已确认 4173、4174、3180、3181 均不再被占用。
- 执行完整类型检查通过。

## 2026-08-17 15:55:03 +0800

- 在员工数据维护的“员工简历（选填）”区域增加“删除简历”按钮；编辑已有员工时确认保存后会清除 PostgreSQL 中该员工的简历文件名、类型和二进制文件数据。
- 删除尚未保存的新选中文件时仅移除待上传文件；删除后仍可重新选择同一文件上传。

## 2026-08-17 16:03:56 +0800

- 对平台前端进行去 AI 风文案调整：移除“老板专属视图”“最高权限”“和助手 · 企业老板”“随时可以提问”等身份宣传式表达。
- 移除员工数据页维护入口旁的“PostgreSQL 数据库”标识，并将数据库实现细节改为中性的系统数据描述。
- 将导航、概览、登录页和员工查询界面的“Agent”“DSH”“虚拟测试数据”等面向使用者的技术或 AI 化表述改为员工查询、员工信息和办公平台语言；不改变员工查询能力及后台集成。
- 将驾驶舱中的“AI 经营分析”改为“经营提示”，保留现有数据展示与参考信息功能。

## 2026-08-17 16:08:12 +0800

- 移除员工查询侧栏的账号“专属空间”说明，以及空闲状态下的“可以开始查询”提示；查询进行中仍显示“正在查询”。

## 2026-08-17 16:09:18 +0800

- 将前端去 AI 风要求写入项目执行约束：统一中性办公语言，隐藏实现与营销术语，移除拟人化、身份宣传和冗余状态文案，并避免 AI 暗示性视觉元素。

## 2026-08-18 14:04:48 +0800

- 将 `data/王叔和在职.xlsx`（82 名在职员工、36 列人事档案）作为正式员工数据源，新增 `data/` 到 Git 忽略，禁止真实身份证、银行卡、住址进入仓库。
- 新增 PostgreSQL `004` 迁移：employees 表增加 25 个个人档案列（所属公司、性别、身份证、出生日期、学历、紧急联系人、银行卡、档案编号、备注、转正与合同日期等），`work_location` 改为可空，身份证增加部分唯一索引；银行卡刻意不加唯一约束（存在两人共用一张卡）。
- 新增 `apps/api/scripts/import-employees.mjs`：解析 Excel（公式取缓存结果、超链接取显示文本、日期按本地时区取值）、`/` 转 NULL、用工类型映射（合同工→full_time、实习协议→intern）、状态按实际转正日期推导；默认 dry-run 只输出问题清单 `data/import-issues.md`（51 项、9 类，含试用期未填写 22 人、转正日期不一致 13 人等，数据均原样导入不自动修改），`--apply` 在单事务中清空 employees 表后全量导入并校准序列。
- `db:seed` 改为执行正式数据导入（破坏性：清空后导入），删除原 `scripts/seed.mjs`；10 名虚构 mock fixture 仅供 Agent 测试与 `validate:data`。
- 领域包 `EmployeeRecord` 追加 25 个可选可空字段，新增 `sensitiveEmployeeFields` 敏感字段单一事实源；API 输入校验、仓储列映射与 INSERT/UPDATE 同步扩展，工作邮箱/身份证唯一冲突按约束返回不同提示，搜索覆盖所属公司、二级部门与个人邮箱。
- Agent 输出白名单未增加任何敏感字段；`postgres-repository` 对可空的 `work_location` 做空串容错，模型无法通过工具获得身份证、银行卡或住址。
- 前端员工数据模块升级：列表新增“合同到期”列（剩余天数，60 天内警示）；详情抽屉改为六组档案卡（任职/身份/联系/教育/财务/备注），年龄、工龄、合同剩余天数由前端动态计算；新建 `MaskedText` 组件，身份证、银行卡、住址默认脱敏（如 `3411**********5817`）点击眼睛图标显示原文；编辑表单补齐全部档案字段（选填）并支持身份证重复校验。
- 新增 8 项前端纯函数测试（脱敏规则、合同剩余天数、年龄、工龄）。
- 更新员工 Agent 规格（新增 2.1 敏感数据政策，撤销“不存储身份证/银行卡/住址”旧声明，薪酬与健康信息仍不存储）、README 员工数据说明、第三方软件声明（ExcelJS）与开发日志。
- 已知遗留：`apps/web/test/accounts.test.mjs` 中老板显示名断言仍为“企业老板”，与去 AI 风后 `accounts.ts` 的“管理员”不一致，本次未改动该测试。

## 2026-08-18 14:19:39 +0800

- 按老板反馈调整员工列表：联系方式列改为手机号在前、邮箱在后；删除直属经理列；“入职 / 地点”列改为“入职时间”，只显示入职日期。
- 调整员工状态推导规则并重新导入 82 人：只有试用期时长有值且尚无实际转正日期的员工才标记“试用期”；试用时长与转正日期均无数据的默认按在职处理。当前 82 人全部为在职状态。
- 员工数据页新增“导出”按钮：导出为 Excel（exceljs 按需动态加载，字段与原 Excel 36 列一致，年龄/工龄/合同剩余天数动态计算）与导出为 PDF（打开横向 A4 打印窗口，通过系统打印对话框另存为 PDF）。导出内容包含身份证、银行卡、住址等完整档案字段，仅限管理者在员工数据页使用。

## 2026-08-18 14:24:41 +0800

- 员工列表排序由姓名改为按入职时间升序（同日按员工 ID），与原始 Excel 顺序一致；搜索过滤时保持同一排序，导出文件同样按此顺序。

## 2026-08-18 14:30:00 +0800

- 修复 API 与 Agent 仓储中 pg date 列经 `toISOString` 输出导致东八区日期偏移一天的既有问题，统一改为本地 getter 格式化；已用林韦康（入职 2022-11-10）、外籍员工 KADALIPURA（入职 2026-07-27）与出生日期、合同到期日期逐项核对，API 返回与 Excel 一致。
- 全量验证：根构建、严格类型检查、数据校验、6 项 Agent 测试、17 项平台测试（16 项通过，1 项为既有 accounts 显示名断言不一致）、8 项新增脱敏/派生计算测试全部通过；`data/` 已被 Git 忽略。

## 2026-08-18 14:31:00 +0800

- 员工数据页新增排序功能：可按入职时间、姓名、部门、合同到期四种条件排序，支持升/降序切换（按钮箭头指示）；默认入职时间升序。无合同到期日期的员工在“合同到期”排序下始终排在最后。排序在前端完成，导出文件跟随当前排序。
- 排序比较逻辑抽取到独立模块 `apps/web/src/modules/employee-agent/employee-sort.ts`，新增 3 项排序测试。

## 2026-08-18 14:42:00 +0800

- 同步更新员工 Agent 运行时：重新构建 `@hegongzuo/employee-agent`、通过 `dsh:accounts:setup` 重装 boss/developer 两个账号的插件 bundle，并重启平台与全部账号专属 DSH 运行时（3180/3181）与平台服务（4173/4174），全部返回 HTTP 200。
- 验证 Agent 数据层已同步正式数据库：`employee_search` 总数为 82 人，入职日期无时区偏移（如 KADALIPURA 2026-07-27），`work_location` 空值容错生效，输出字段白名单不含身份证、银行卡、居住住址、身份证地址四类敏感字段；`organization_list_members`（品牌中心）返回 25 人。

## 2026-08-18 14:55:00 +0800

- 员工查询 Agent 的字段能力与正式数据库同步：工具输出白名单新增 17 个非敏感档案字段（所属公司、二级部门、性别、出生日期、学历、专业、毕业学校、毕业时间、婚否、育否、籍贯、试用期与转正日期、合同到期日期、档案编号、备注），工具输出 Schema、仓储查询与搜索匹配（所属公司/二级部门/学历/学校）同步扩展；输出字段从 16 个增至 33 个。
- 受限字段边界明确写入系统提示：身份证、银行卡、居住住址、身份证地址、紧急联系人及电话、个人邮箱不进入工具输出，模型无法查询。
- 重新构建插件、重装 boss/developer 运行时并重启平台，全部服务 HTTP 200；验证“完素”搜索命中张发恒、受限字段零泄漏、新字段值正确。

## 2026-08-18 15:20:00 +0800

- 员工查询 Agent 新增两个只读工具：
  - `employee_stats`：员工总体分布统计（总人数 + 按所属公司、部门、学历、性别、用工类型、状态、年龄段、司龄段分组计数，同人数按中文拼音排序）。
  - `contract_alerts`：合同与转正预警（合同在 N 天内到期的员工、已过期未续签员工、预计转正日期临近但尚未实际转正员工；窗口默认 60/30 天可调，按剩余天数升序）。
- 两个工具复用现有快照仓储，无新增查询路径；输出不包含任何敏感字段。
- 更新系统提示说明新工具用途；Agent 测试扩展为 8 项全部通过；重新安装插件并重启 boss/developer 运行时，全部服务 HTTP 200。
- 真实数据验证：统计口径 82 人（工程 58/医药 22/完素 1/子和 1，本科 39、女 42/男 40）；60 天合同预警命中 4 人（郭春霖 2026-08-21 仅剩 3 天、代琛琛、曾维威、张颖）。

## 2026-08-18 15:40:00 +0800

- 员工查询主界面删除 3 个过时快捷问题，替换为四宫格功能介绍（员工查询、组织关系、统计分布、合同与转正预警），文案保持中性办公语言。
- 新增轻量 Markdown 渲染组件 `markdown.tsx`（无第三方依赖）：Agent 回答中的加粗、斜体、行内代码、表格、无序列表、代码块与段落正确渲染，所有文本先做 HTML 转义防止注入；assistant 消息统一改用该组件渲染。
- 新增渲染样式：表格横向滚动、代码块底色、加粗正文等，沿用设计变量。

## 2026-08-18 15:55:00 +0800

- 全链路删除“直属经理”概念：新增 `005` 迁移删除 employees 表 `manager_id` 列（连同外键、约束与索引），数据保持 82 名员工不变。
- 同步清理 API 输入校验/仓储 SQL/服务端路由、员工领域模型、Agent 工具输出 Schema 与投影、前端类型/编辑表单/档案卡/删除逻辑、mock 测试数据与数据校验脚本中的 managerId/managerName；`organization_list_members` 描述中不再承诺汇报关系。
- 更新 Agent 测试（删除经理解析断言），全量构建、严格类型检查、数据校验、8 项 Agent 测试全部通过；平台测试 19/20（1 项既有 accounts 断言问题）。

## 2026-08-18 16:33:28 +0800

- 按《和工作平台设计规范》（玉青白 · 王叔和品牌风格）重构前端视觉：
  - `tokens.css` 重写为玉青白色板：深墨青 `#163F3A`、交互青绿 `#2F8C78`、王叔和玉青 `#4FA38E`、淡玉青 `#EDF7F4`、页面背景 `#F5F8F7`，新增成功/警告/危险状态色（`#16835D`/`#C47A20`/`#C84B4B`），圆角收敛为 8/12/16px，阴影改为浅青绿投影；旧变量名保留为新值别名。
  - 登录页由深藏青渐变改为青灰白浅色品牌区；侧边栏由深色改为白色 + 淡玉青选中态（规范不建议整块深色侧边栏）；概览 Hero 与驾驶舱深色卡片改为深墨青渐变。
  - 品牌标记去掉 Sparkles 图标（规范禁止 AI 暗示元素），保留纯几何图形，渐变换为王叔和玉青系。
  - 驾驶舱指标卡由彩色系收敛为品牌色系；全部模块 CSS 中的硬编码蓝灰色替换为新色板（表格表头、输入边框、focus ring、错误/警告状态色等）。
  - 基础字号按规范提升（辅助信息 11-12px、表格正文 12px+），保持紧凑但不缩小可读性。
- 验证：类型检查、生产构建、平台测试（19/20，1 项既有）通过；运行中平台热更新已生效。

## 2026-08-18 16:50:00 +0800

- 使用《和工作logo.png》作为平台 Logo：图片优化为 256px 版本（68 KB）放入 `apps/web/public/logo.png`，`BrandMark` 组件改为图片引用，删除原 CSS 几何图形 Logo（含渐变方块与旋转线条样式）。
- `index.html` 增加 favicon 引用，浏览器标签页同步显示新 Logo。
- 验证：类型检查、生产构建通过，运行中平台可直接访问 /logo.png（HTTP 200），刷新页面生效。

## 2026-08-18 17:05:00 +0800

- 老板账号信息更新：显示名由“管理员”改为“陶春霖”，角色标识由“老板”改为“CEO”（首席执行官），顶栏与登录提示不再显示账号英文标识；概览问候改为“你好，陶总”，其他账号显示“你好，{姓名}”。
- 同步修复既有 accounts 测试断言（显示名），平台测试 20/20 全部通过；README 账号表更新为陶春霖（CEO）。登录账号 `boss` 仅作为内部标识保留。

## 2026-08-18 17:12:00 +0800

- 开发者账号显示名由“平台开发者”改为“刘奥”。
- 顶栏头像标识由角色固定字（“和”）改为按登录用户姓名首字动态显示（陶春霖→陶、刘奥→刘），不做硬编码。
- 登录提示卡此前已改为显示姓名（陶春霖 / 刘奥），刷新页面后不再出现英文账号标识；README 账号表同步。

## 2026-08-18 17:40:00 +0800

- 账号系统阶段 A（认证闭环）上线：
  - 新增 `006` 迁移：`accounts` 表（登录名、显示名、scrypt 密码哈希、角色 owner/developer、启用状态）与 `sessions` 表（7 天有效期）。
  - 新增账号种子脚本（`db:seed:accounts`，并入 `db:setup`）：初始账号陶春霖（`taochunlinxx`/owner）、刘奥（`liuao01`/developer），初始密码 demo123。
  - 新增认证接口：`POST /api/auth/login`（登录名+密码→会话 token）、`GET /api/auth/me`（恢复登录态）、`POST /api/auth/logout`（销毁会话）；账号不存在与密码错误返回统一提示。
  - 员工数据 API（列表/详情/新增/更新/删除/简历）全部要求 Bearer token，未登录返回 401。
  - 前端：删除本地账号校验（`accounts.ts` 与相关测试），登录页对接认证接口，登录态存 localStorage（7 天自动恢复，401 自动回登录页）；顶栏退出调 logout。
  - 修复迁移全量重跑问题：`001` 中已删除列（manager_id）的建表语句与索引移除，保持迁移链幂等。
  - 平台测试 13/13 通过；认证流程（登录/me/带 token 访问/无 token 401/登出）经 API 实测全部通过。

## 2026-08-18 18:30:00 +0800

- 账号系统阶段 D（Agent 运行时联动）上线：
  - 新增 `dsh:accounts:sync` 脚本：从 accounts 表读取启用账号，按创建顺序从 3180 起分配端口，生成 `.runtime/account-agent-runtimes.json`（Git 忽略）；`dsh:accounts`、`dsh:accounts:setup` 与平台启动器统一先同步再启动。
  - 旧静态配置 `config/account-agent-runtimes.json` 删除；前端运行时解析与 Vite 代理改为读取生成文件。
  - 账号目录自动迁移：boss/developer 的 DSH_HOME 与工作区按端口顺序改名为新登录名（taochunlinxx/liuao01），会话历史随目录保留。
  - 端到端验证：新账号创建后 sync 自动分配 3182 与 `/dsh/<登录名>` 代理路径；账号隔离验证通过（独立 DSH_HOME、工作区、端口）；四服务全部 HTTP 200。
  - 当前模式为全量启动（平台启动时拉起全部启用账号的运行时），新增账号后重启平台生效；懒启动留待部署阶段。

## 2026-08-18 18:55:00 +0800

- 账号规则与密码流程调整：
  - 登录名规则简化为姓名拼音（小写字母开头，可含数字），不再要求年份简写；现有账号迁移为 taochunlin / liuao，DSH 目录与运行时配置同步改名，会话历史保留。
  - 新建账号不再填写初始密码，统一为默认密码 wangshuhe123；开发者“重置密码”改为“一键重置为默认密码”（确认后直接生效）。
  - 新增“修改密码”：任何账号登录后可从顶栏进入修改自己的密码（校验当前密码、新密码至少 6 位、二次确认），修改后旧密码即时失效。
  - 账号管理界面同步：创建表单去掉密码输入、登录名规则提示更新、重置按钮文案与确认流程更新。
  - 全流程实测通过：自行改密码（旧密码失效/新密码登录）、一键重置后默认密码登录、新建账号默认密码登录、新登录名代理路径（/dsh/liuao）与账号隔离验证通过；平台测试 13/13。
  - 修复 sync 脚本迁移源：目录迁移改为对比上一版生成文件（支持登录名再次变更时自动迁移）。

## 2026-08-19 09:32:00 +0800

- 账号系统的"角色"改为"职位"：accounts 表新增 `position` 列（迁移 007），职位为自由文本（如 CEO、财务经理），不再限制为开发者/CEO 两个下拉选项；创建/编辑账号表单改为职位文本框 + "开发者权限"复选框（勾选即 developer，可管理账号与开发控制台）。
- 顶栏、账号管理表格、登录页文案同步改用职位：顶栏显示职位（职位为空时回退原角色标签），表格"角色"列拆为"职位"+"权限"列（普通/开发者徽标），概览问候语与默认首页改为按职位 CEO 判断（"你好，陶总"仅对 CEO 职位显示），管理驾驶舱仅 CEO 职位可见（module-registry 增加 bossOnly 标志）。
- 迁移 007 已应用，现有账号补职位：陶春霖=CEO（owner）、刘奥=开发者（developer）；创建账号接口增加职位必填校验，API/前端类型检查与 13 项测试全部通过。

## 2026-08-19 10:00:00 +0800

- 账号管理新增"删除账号"功能（仅开发者可用）：
  - 后端 `DELETE /api/accounts/:id`：禁止删除当前登录账号（400）；账号不存在返回 404；硬删除（accounts 表无软删除标记）。
  - 前端账号管理表格操作列新增"删除"按钮（红色边框，`account-admin__danger`），点击弹确认框（提示不可恢复、登录会话与专属 Agent 运行时都会停止），删除成功即时从列表移除；当前登录账号的删除按钮禁用。
  - `dsh:accounts:sync` 脚本新增已删账号的目录清理：旧配置有、当前账号列表没有的账号，其 `.runtime/dsh/<账号>` 与 `.runtime/workspaces/<账号>` 自动删除；清理放在迁移之后并先检查目录存在，避免误删登录名修改（已迁移改名）的目录与误打印清理日志。
  - 迁移守卫与边界：目录迁移仅在账号数量不变时执行（登录名修改场景）；已知边界为等数量"删除+新增"组合无法与登录名修改区分，按迁移处理（数据保留而非删除，不丢数据；真实使用中删增之间有中间同步，几乎不会触发）。
  - API 实测通过：删除成功（ok:true）、删除不存在账号 404、自删 400 拒绝；同步脚本三个场景隔离测试通过（登录名修改迁移 / 删除账号清理 / 删+增边界不误删）；前端类型检查通过。

## 2026-08-19 13:32:35 +0800

- 将开发日志加入 `.gitignore`，后续日志不再进入 Git 变更列表。
- 优化账号管理界面排版：账号表格增加明确边界与最小宽度，操作按钮可换行且保持文字完整；小屏幕下标题区与新增按钮改为纵向排列。
- 修复账号编辑表单中“开发者权限”复选框继承通用输入框样式而导致图标、边框和文字重叠的问题；该控件现使用独立横向布局和边框样式。

## 2026-08-19 13:36:38 +0800

- 保持账号编辑表单字段的竖向排列，并保留“开发者权限”复选框的独立样式修复。
- 重构账号管理顶部区域：将标题、说明和新增账号入口整合为带独立边界的管理头部，表格区域使用一致的内边距与间距；小屏幕下头部改为纵向布局。

## 2026-08-19 13:39:12 +0800

- 修正账号新增/编辑表单的 CSS 选择器优先级：单列规则现覆盖通用双列表单规则，姓名、账号名、职位和开发者权限复选框固定按竖向排列。

## 2026-08-19 13:41:32 +0800

- 将当前登录账号的姓名、职位和账号名与主界面右上角资料绑定：在账号管理中保存当前账号后，平台状态会立即同步更新，顶栏无需重新登录即可显示最新资料。

## 2026-08-19 13:46:57 +0800

- 清除 CEO 与开发者工作台中展示用的模拟经营数据、测试数据统计和预设数值；对应位置统一显示“待开发”。
- 管理驾驶舱保留模块结构与业务分类，不再绘制模拟趋势、异常、状态和经营提示内容，等待后续接入真实数据。
- 删除已不再被页面使用的管理驾驶舱模拟数据源文件，避免项目中残留展示用模拟经营文字。

## 2026-08-19 14:56:48 +0800

- 修复账号资料同步回调的严格 TypeScript 可选属性错误：仅在回调存在时向账号管理模块传递该属性，恢复前端构建。

## 2026-08-19 15:06:00 +0800

- 新增离职人员档案导入：读取 `data/王叔和离职.xlsx` 的 194 条记录，以离职状态增量写入员工档案，不清空现有在职员工数据；新增离职日期与离职原因字段，并允许同一人员保留多段任职记录。
- 员工信息页面新增全部、在职、离职筛选和人数汇总；离职日期、离职原因进入员工档案任职信息中展示。
- 导入发现离职表存在富文本邮箱单元格及跨任职记录的重复企业邮箱；导入事务已自动回滚。新增兼容解析和任职记录邮箱约束调整后将重新执行增量导入。
- 导入过程中发现离职日期列含有附带说明的文本日期；补充日期提取规则，导入事务继续保持原子回滚保护。
- 数据迁移与第三次导入成功：新增 194 条离职档案；数据库现有在职 82 条、离职 194 条。补齐共享和前端员工类型中的离职字段。

## 2026-08-19 15:18:04 +0800

- 员工信息页移除“全部”筛选，仅保留在职和离职两类，默认显示在职人员。
- 离职人员表改用离职档案列：入职时间、离职时间及实际工龄、离职原因；不再显示用工状态、合同到期等在职列。

## 2026-08-19 15:18:04 +0800

- 重新排版离职人员视图：页面标题与汇总随在职/离职切换，离职表的入职、离职和工龄信息使用清晰的两行档案布局，离职原因列加宽并支持长文本换行。
- 核对离职 Excel：32 条离职原因是数字，其中 31 条转换为 Excel 日期后与同一行离职日期一致，确认属于源表日期误填而非真实离职原因；本次未擅自篡改原始数据。

## 2026-08-19 15:23:18 +0800

- 将离职原因中的 Excel 日期序列恢复为标准日期：新增历史数据修正脚本，并在离职导入流程与前端显示层同步加入转换规则，避免再次出现数字日期。

## 2026-08-19 15:26:56 +0800

- 员工信息列表新增分页：默认每页展示 20 人，提供上一页、下一页与页码提示；切换在职/离职、搜索或排序时自动回到第一页。

## 2026-08-19 15:29:26 +0800

- 员工列表分页默认改为每页 10 人，并新增每页 10、20、50、100 人的选择器；修改每页数量后自动回到第一页。

## 2026-08-19 15:31:09 +0800

- 移除员工信息页返回入口与主标题之间的“员工数据”眉题及图标，并统一移除登录页、概览、员工查询、管理驾驶舱和开发控制台中的同类小标题。

## 2026-08-19 15:45:06 +0800

- 员工列表切换上一页或下一页后，页面自动平滑滚动至该页表格顶部，直接定位到新页第一条员工记录。

## 2026-08-19 15:47:58 +0800

- 将离职日期和离职原因从通用任职信息中拆分为独立“离职信息”分组，仅在离职员工档案中展示；在职员工档案不再显示离职字段。

## 2026-08-19 15:50:41 +0800

- 按在职与离职 Excel 的实际字段彻底拆分档案：离职档案移除合同到期、试用期、预计/实际转正、工作地点、职责和简历等在职专用字段；维护编辑界面对离职人员同步隐藏这些字段。

## 2026-08-19 15:59:54 +0800

- 员工管理支持按当前在职或离职页面导出对应字段的数据表，并新增员工离职流转：在职员工可填写离职日期和离职原因后自动转入离职人员档案，原有员工资料保持不变。

## 2026-08-19 16:05:02 +0800

- 移除员工档案的删除按钮、前端删除请求和服务端删除接口；人员记录仅可通过新增入职或员工离职发生变动。
- 重新编排离职人员列表为“员工与岗位、联系方式、任职周期、离职原因、档案”五列，突出任职起止日期、工龄与离职原因。

## 2026-08-19 16:07:07 +0800

- 离职人员列表改为与在职员工一致的七列表格布局；离职列表仍仅展示离职专属的入职时间、离职时间与工龄、离职原因字段。

## 2026-08-19 16:11:11 +0800

- 按要求从离职员工数据中删除“张三”一条记录（EMP-0352）。

## 2026-08-19 16:15:47 +0800

- 更新员工管理插件与页面能力介绍：新增只读离职员工查询能力，查询结果包含离职日期和离职原因；插件说明、系统提示和页面能力卡同步说明在职与离职员工查询范围。

## 2026-08-19 16:17:04 +0800

- 优化员工查询首页的能力介绍布局：能力卡片使用统一编号、清晰的标题与说明层级、两列自适应网格和更明确的悬停反馈，小屏幕自动改为单列。

## 2026-08-19 16:17:50 +0800

- 调整能力介绍卡片排列：前四项保持两列两行，最后一项独占整行，避免两侧卡片数量不一致。

## 2026-08-19 16:19:33 +0800

- 统一能力介绍卡片的标题行与说明行高度，并改为顶部对齐，消除不同文字长度造成的内容错位。

## 2026-08-19 16:34:27 +0800

- 新增生产部署层：员工查询统一经平台 API 校验登录会话后转发至账号对应的本机运行时，支持普通请求与实时连接，浏览器不再持有运行时地址。
- 新增生产启动命令、systemd 服务模板、Nginx HTTPS 反向代理模板和单机 Ubuntu 部署说明；前端改为由 `/api/employee-agent` 统一访问员工查询服务。

## 2026-08-19 16:55:10 +0800

- 移除前端对账号运行时配置文件的编译期依赖；员工查询运行时现完全由平台 API 在服务端按登录账号解析，生产构建不再要求预先生成 `.runtime` 文件。

## 2026-08-19 17:14:19 +0800

- 为公网 IP 的 HTTP 临时访问补充 `crypto.randomUUID` 兼容实现，修复员工查询界面在非 HTTPS 浏览器环境中无法初始化的问题。

## 2026-08-19 17:15:19 +0800

- 修正 UUID 兼容实现的严格 TypeScript 数组下标检查，恢复生产构建。

## 2026-08-20 09:25:00 +0800

- 新增账号模块权限：员工档案维护与员工查询可分别开通；平台按权限显示入口并校验数据与查询请求。
- 新增账号创建后的自动初始化流程：已开通员工查询的账号会自动创建独立运行空间并加入运行服务；运行服务支持读取配置变更，单个账号初始化或运行失败不再终止平台与其他账号服务。

## 2026-08-20 09:35:00 +0800

- 修正员工个人档案迁移：身份证号存在历史重复时不再创建唯一索引，避免本地数据库迁移中断。

## 2026-08-20 09:40:00 +0800

- 修正账号查询运行服务的本地环境加载，确保独立运行空间能够读取数据库连接配置。

## 2026-08-20 09:50:00 +0800

- 重构侧边栏业务管理结构：员工档案与员工查询归入员工管理；新增财务管理、项目管理入口并显示待开发。
- 账号权限新增财务管理与项目管理开关，所有业务管理入口均按账号已开通权限显示。
- 调整侧边栏分组的显示顺序，使员工管理目录保持在概览之后、财务管理与项目管理之前。

## 2026-08-20 10:00:00 +0800

- 将员工管理、财务管理、项目管理统一改为侧边栏可展开和收起的目录结构；员工管理内保留已有员工档案与员工查询入口。

## 2026-08-20 10:10:00 +0800

- 新增 hgzuo.com 与 www.hgzuo.com 的 HTTPS Nginx 部署配置，准备启用证书访问。

## 2026-08-20 10:25:00 +0800

- 离职员工列表默认按离职时间降序排序，并在离职视图中提供离职时间排序选项。
- 新增组织架构图：基于在职员工的一级部门、二级部门与岗位展示组织分布，并支持通过打印窗口导出为 PDF。

## 2026-08-20 10:40:00 +0800

- 为旧版浏览器补齐员工查询连接所需的 AbortSignal.timeout 与 AbortSignal.any 兼容实现，修复不支持该 Web API 时的查询初始化失败。

## 2026-08-20 10:55:00 +0800

- 组织架构图改为以公司、一级部门、二级部门和员工岗位呈现的思维导图布局，基于当前在职员工数据实时生成。
- 移除组织架构页面重复的返回员工信息操作；重构新增账号表单的基本信息与功能权限分区布局。
- 新增项目版本发布规则：小变动递增 `.0x`，中等变动递增 `.x0`，大变动递增 `x.00`。

## 2026-08-20 11:51:00 +0800

- 登录态改为仅通过 HttpOnly、SameSite=Strict 会话 Cookie 传递，移除浏览器 localStorage 中的登录令牌及 Bearer Token 使用。
- 新增登录来源失败限流与账号连续失败临时锁定，并在 HTTPS 反向代理下自动为会话 Cookie 启用 Secure 属性。
- 同步员工查询插件测试与现有离职查询能力，覆盖六项已注册工具。
- 移除服务端 Bearer Token 兼容入口，使浏览器历史遗留的本地令牌无法再用于认证。

## 2026-08-20 13:25:34 +0800

- 精简根目录 `AGENTS.md`：合并重复表述，保留执行范围、模块化、前端语言、安全日志与发布约束的完整要求，降低项目级指令的上下文占用。

## 2026-08-20 13:33:19 +0800

- 将员工数据管理从单一页面组件拆分为页面编排、员工列表、员工编辑器、状态管理与数据请求文件；组织架构继续保持独立组件和数据构建边界。
- 保持模块注册、权限配置和现有员工查询、维护、离职、组织架构及导出入口不变，降低后续功能扩展时的耦合范围。

## 2026-08-20 13:38:40 +0800

- 将员工数据维护的入口、页面、列表、编辑器、状态管理、数据访问、导出和组织架构文件迁移至独立的 `modules/employee-data` 模块目录。
- 更新模块注册表的入口引用，并移除员工查询模块内直接进入员工数据维护页面的耦合入口；员工数据模块仍通过注册表统一提供导航和权限控制。

## 2026-08-20 13:42:52 +0800

- 更新员工数据测试的模块导入路径，使其引用独立 `modules/employee-data` 模块中的领域与排序文件。

## 2026-08-20 13:49:06 +0800

- 重组 Web 模块目录为“主模块 / 子功能”结构：员工模块包含 `agent` 与 `data`，管理模块包含 `cockpit` 与 `placeholder`；财务、项目、开发控制台和概览也迁移至各自的主模块目录。
- 更新模块注册表、模块内引用和 Web 测试导入路径，保留原有模块 ID、导航名称和权限配置不变。

## 2026-08-20 13:55:42 +0800

- 建立后端 HTTP 与认证权限边界，迁移员工输入校验和 PostgreSQL 仓储至 `apps/api/src/modules/employee`，为后续按业务模块拆分路由和服务提供目录基础。

## 2026-08-20 14:05:00 +0800

- 将账号运行环境初始化改为后台串行任务队列；账号新建和更新接口立即返回初始化状态，不再等待外部脚本完成。
- 新增账号初始化重试接口 `POST /api/accounts/:id/retry-initialization`；任务失败后保留 `initialization_failed` 状态供管理员重试。
- 开发控制台账号列表保留初始化状态展示，并在初始化失败时提供“重试初始化”操作。

## 2026-08-20 14:12:00 +0800

- 同步 Web 模块目录重组后的应用层与占位模块导入路径，并修正员工查询图标与员工数据校验的可空类型边界。

## 2026-08-20 14:20:00 +0800

- 员工列表改为数据库执行在职/离职筛选、关键词搜索、排序和分页；接口返回当前页与总数，前端不再在浏览器中加载和分页全部员工记录。

## 2026-08-20 14:25:00 +0800

- 修正员工列表分页容器的 React `ref` 严格类型，使其可作为 DOM 引用传递。

## 2026-08-20 14:35:00 +0800

- 新增按当前筛选与排序条件获取完整员工数据的导出接口，Excel 和 PDF 导出不再受列表分页限制。
- 确认 Excel 导出已使用动态加载；构建警告中的独立 Excel 资源不再进入首屏主包。

## 2026-08-20 14:45:00 +0800

- 扩展 API 健康检查：除数据库外，探测所有已启用员工查询权限账号的本地运行时；任一运行时不可用时 `/health` 返回 503 与运行时汇总状态，避免监控误报正常。

## 2026-08-20 15:00:00 +0800

- 为生产 systemd 服务模板增加 CPU、内存、任务数、文件句柄与文件系统写入边界；服务失败和健康检查失败均可触发可配置告警。
- 新增 API、PostgreSQL 和账号运行时的定时独立健康检查单元与脚本；健康状态异常由 systemd timer 检出，不依赖自动重启。
- 新增 journald 日志容量、单文件大小和保留期限模板，避免生产日志无限增长。

## 2026-08-20 15:15:00 +0800

- 新增独立 API systemd 服务模板与按账号实例化的员工查询运行时模板，分别设置资源和故障边界。
- 新增单账号运行时启动脚本，并补充从组合服务迁移至独立服务的部署步骤。

## 2026-08-20 15:19:00 +0800

- 建立通用 Agent 运行时注册机制：扫描 `packages/*/hegongzuo-agent.json`，按账号权限生成统一运行时配置，并使用 `agent-id--account-id` 独立标识实例。
- 员工查询 Agent 已迁移为首个清单实例；账号初始化、本地运行监督、生产 systemd 同步与平台健康检查均改为读取统一注册表。
- 新增 Agent 注册约定文档；后续符合 `dsh-web` 清单规范的 Agent 在部署和账号权限变更后将自动初始化、启停并纳入健康检查。
- 生产同步器在升级时先自动停用旧版“仅账号名”运行实例，再启动新实例，避免端口争用。

## 2026-08-20 15:25:00 +0800

- 新增生产 systemd 模板核查命令，并在部署文档中加入预发布故障演练、生产迁移验证和回滚步骤。

## 2026-08-20 15:27:00 +0800

- 修正 API 重启时可能被长连接阻塞的问题：收到停止信号后先停止接收新请求，5 秒后关闭未结束连接，再关闭数据库连接，保证部署可在 systemd 的停止期限内完成。

## 2026-08-20 15:30:00 +0800

- 修正生产 Agent 同步器对 systemd 单元列表前导空格的解析，确保升级时能够识别并停用旧实例、启动新的 `agent--account` 实例。

## 2026-08-20 15:35:00 +0800

- 修正生产服务模板核查脚本的严格 TypeScript 数组下标可空错误，避免其阻断平台开发启动前的统一构建。

## 2026-08-20 15:35:00 +0800

- 重建员工管理的组织架构图：改为带连接线的树状层级，依次展示组织、一级部门、二级部门与员工岗位；支持按部门和组别展开或收起，并保留 PDF 导出。

## 2026-08-20 15:45:00 +0800

- 将离职人员列表的排序选项调整为“离职时间”和“工龄”，并在数据库分页查询与导出查询中同步按离职日期减入职日期计算工龄排序。

## 2026-08-20 15:45:00 +0800

- 删除组织架构页面标题下的说明文案，避免与图表内容重复。

## 2026-08-20 15:50:00 +0800

- 删除 `.gitignore` 中会匹配任意同名目录的全局 `data/` 规则；本地运行时数据仍由 `.runtime/` 规则忽略，员工管理源码目录不再被错误排除。

## 2026-08-20 15:50:00 +0800

- 组织架构图改为独立加载全部在职员工，不再复用分页列表的当前 10 条数据；增加加载中、加载失败与重新加载状态。

## 2026-08-20 15:55:00 +0800

- 调整组织架构图为一级部门节点横向排列、展开内容纵向排列；PDF 导出同步为所有节点默认展开的完整树状图。

## 2026-08-20 15:58:00 +0800

- 修正独立 systemd 服务模板：将 `OnFailure` 置于 `[Unit]`，并让独立 API 直接运行已构建的 Node 入口，避免 `ProtectHome` 阻断 Corepack 缓存访问。

## 2026-08-20 16:05:00 +0800

- 优化组织架构树的横向布局：根节点至部门节点的连接线完整连续，部门节点等宽对齐；调整节点间距、展开内容密度与窄屏横向浏览体验。

## 2026-08-20 16:10:00 +0800

- 新增受控账号运行时同步器：systemd Path 单元监听运行时配置，root 所有的最小权限脚本根据严格校验的账号 ID 自动启停账号实例，API 不直接获得 systemd 权限。

## 2026-08-20 16:10:00 +0800

- 为 API 停止流程增加数据库连接池关闭的 5 秒上限，到期后明确退出进程，避免自动部署被遗留数据库连接阻塞。

## 2026-08-20 16:15:00 +0800

- 修正员工信息页的在职、离职人数统计：标题改为使用服务端分页接口返回的总人数，不再错误显示当前页的 10 条记录数。

## 2026-08-20 16:20:00 +0800

- 修正员工在职/离职切换时的旧数据闪现：列表仅显示与当前筛选条件匹配的响应，过期请求结果不再覆盖新状态；加载期间显示统一的加载提示。

## 2026-08-20 16:30:00 +0800

- 优化员工在职/离职切换体验：按查询、范围、页码与排序缓存列表结果，并后台预加载另一范围的首页；已访问范围切换时即时显示缓存并静默刷新。

## 2026-08-20 16:31:00 +0800

- 修正员工查询消息被普通接口 30 秒时限提前取消的问题：短请求仍使用 30 秒健康时限，消息任务独立使用 5 分钟客户端时限与 5 分 30 秒代理时限。
- 优化消息完成后的会话同步：实时事件已到达时不再重复加载整段历史，仅在未收到实时事件时兜底同步，减少长对话的重复传输和渲染。
- 新增员工聚合分析工具，直接按状态、性别、部门和岗位计算平均年龄、平均离职年龄与平均工龄；约束查询策略不得分页搬运全部明细后调用脚本计算。
- 全项目构建及 24 项自动化测试通过。

## 2026-08-20 16:35:00 +0800

- 修正员工查询实时连接在浏览器断开时可能触发未处理 `ECONNRESET` 并退出 API 的问题：代理层现在接管 Socket 错误、双向清理管道，并仅关闭当前会话连接。

## 2026-08-20 16:42:00 +0800

- 修复员工查询流式 Markdown 表格在分隔行尚未到达时重复解析同一行、持续创建节点并耗尽浏览器内存的问题。
- 将消息分块解析提取为独立模块，每轮解析强制推进游标；补充半成品表格、完整表格及无尾部竖线表格的回归测试。
- 全项目构建及 28 项自动化测试通过。

## 2026-08-20 16:45:00 +0800

- 完成员工查询链路稳定性优化：网页端对实时事件按 80ms 批量合并，降低流式回答期间的重复渲染；初始化失败自动退避重试，连接取消不再显示底层异常文案。
- 实时连接增加 10 秒建连上限与浏览器错误处理，普通代理请求增加 30 秒响应上限和上下游断连清理，避免悬挂连接持续占用资源。
- 会话请求超时由 20 秒调整为 30 秒；连接恢复后自动清理临时错误状态，历史中断提示改为面向使用者的可操作文案。
- 新增流式事件批量合并和连接取消提示测试；全项目构建及 23 项自动化测试通过。

## 2026-08-20 17:00:00 +0800

- 重构组织架构图视觉层级：一级部门保持横向排列，二级部门与员工纵向展开；统一为中性办公配色，优化节点图标、人数徽标、员工头像、留白、连线和横向浏览体验。
- PDF 导出同步采用新的组织架构视觉样式，并保持所有节点默认展开。
- 全项目构建及 28 项自动化测试通过。

## 2026-08-20 17:08:00 +0800

- 将组织架构一级部门由单行横向树改为屏幕内自适应多列网格，窄屏自动切换为单列，取消页面横向滚动。
- 移除部门、组别、员工与根节点的装饰图标，保留层级、人数和展开操作；PDF 同步改为三列网格排版。
- 全项目构建及 28 项自动化测试通过。

## 2026-08-21 09:00:00 +0800

- 在员工管理模块新增企业微信回调安全接口，用于完成“接收消息服务器 URL”校验及后续应用事件的签名、加密和企业标识验证。
- 回调接口不保存聊天消息或日报内容；企业微信原生日报将在后续独立定时同步任务中读取。
- 补充企业微信回调配置说明和校验、签名伪造拒绝的自动化测试；Token、加密密钥和企业标识仅从服务器环境变量读取，不写入仓库。

## 2026-08-21 10:20:00 +0800

- 修正员工查询回复中 HTML 实体被重复转义而显示为 `&quot;` 等字面文本的问题；消息仍以 React 纯文本节点渲染，不插入 HTML。
- 增加常见及无效 HTML 实体的回归测试。

## 2026-08-21 10:40:00 +0800

- 模块注册表新增稳定页面地址，导航改为浏览器历史记录驱动；支持直接访问、刷新保留、收藏分享及浏览器前进后退。
- 无效或无权限地址会安全回退至当前账号默认模块；侧栏模块入口改为可在新标签页打开的链接。

## 2026-08-22 09:00:00 +0800

- 组织架构 PDF 导出调整为仅展示一级部门、二级部门及对应人数，不再展开员工姓名和岗位节点。

## 2026-08-22 09:30:00 +0800

- 取消账号“老板/开发者”角色作为功能访问依据，账号改为按员工、财务、项目、管理驾驶舱、平台管理等功能权限独立授权。
- 账号管理与平台管理入口改为“平台管理”权限控制；管理驾驶舱同时要求该功能权限及 CEO 职位。
- 新增数据库迁移：保留既有开发者的平台管理能力、为 CEO 开通驾驶舱权限后移除旧角色字段。

## 2026-08-22 09:45:00 +0800

- 优化新增和编辑账号中的功能权限布局：按员工管理、财务管理、项目管理和其他分组展示；员工权限改为“档案维护、数据查询”，财务与项目预留项明确标记为“待开发”，驾驶舱与平台账号管理归入“其他”。

## 2026-08-22 10:25:00 +0800

- 将平台管理由静态说明升级为可操作后台：新增实时服务健康状态、模块启用/停用、账号管理操作审计记录与模块访问控制；停用模块后同步隐藏入口并拒绝对应业务接口访问。

## 2026-08-22 11:15:00 +0800

- 修复账号编辑或初始化时员工查询服务被同步器错误清理的问题：初始化中的已授权账号保持运行时配置，账号恢复正常后再次同步最终配置，避免页面出现连接中断。

## 2026-08-22 11:30:00 +0800

- 修复独立 API 与账号运行时服务的 systemd 沙箱阻断 Corepack 缓存问题：在保持其余用户目录隔离的前提下，只读挂载已安装的 Corepack 缓存，避免账号初始化被错误标记为失败。

## 2026-08-22 11:50:00 +0800

- 修复账号权限编辑会错误触发运行时初始化并中断登录的缺陷：仅在员工查询权限或账号名实际变化时同步运行空间；普通功能权限调整不再改变账号状态。登录提示现明确区分停用、初始化中与初始化失败。

## 2026-08-22 12:05:00 +0800

- 将账号运行时变更判断改为自动读取全部 Agent 清单：后续新增 Agent 后，其权限开通、移除或账号名变更都会自动同步对应运行空间，且不会影响账号登录状态。

## 2026-08-22 13:40:00 +0800

- 移除员工档案在职、离职列表的 PDF 导出入口与实现，仅保留 Excel 导出；组织架构等其他导出能力不受影响。

## 2026-08-22 14:10:00 +0800

- 在员工管理中新增合同到期站内提醒：对在职且未来 7 天内到期的合同进行顶部提示，展示员工、部门、岗位、到期日与剩余天数；仅拥有“档案维护”权限的账号可见和访问。

## 2026-08-22 14:25:00 +0800

- 调整合同到期提醒范围：由仅未来 7 天改为已逾期 7 天至未来 7 天，并清晰标注已逾期天数，避免遗漏刚到期的合同。

## 2026-08-22 14:40:00 +0800

- 为员工查询单个对话增加 30 轮上限：达到上限后停止继续发送，明确提示新开对话，并在输入区显示当前轮数；运行时同时校验该限制，避免界面绕过。

## 2026-08-22 14:55:00 +0800

- 为员工查询增加会话与工具返回保护：每个账号最多保存 3 个对话，达到上限后要求先删除已结束对话；部门成员与预警名单单次最多返回 10 条，员工明细和离职查询保留原有最多 50 条的分页能力，并收敛员工字段，降低长对话和大结果集造成的运行压力。

## 2026-08-22 15:10:00 +0800

- 按使用要求撤销员工查询工具的字段精简与部门成员、合同/转正预警名单的 10 条限制，恢复原有完整允许字段与返回能力；会话数量和轮次保护保持不变。

## 2026-08-22 16:10:00 +0800

- 将账号、权限、运行空间与运行服务改为基于 `packages/*/hegongzuo-agent.json` 的通用自动协调机制：新建账号或新增能力包时，按已授权能力自动生成并初始化对应的独立运行空间。
- API 启动及每两分钟自动恢复未完成初始化的账号；生产环境新增十分钟运行空间协调任务，持续修复配置或运行空间缺失，并触发运行服务同步。
- 账号权限不再受固定枚举限制；账号管理自动读取能力包清单并在“扩展功能”中展示新增能力，统一代理入口支持 `/api/agents/<agent-id>`，后续能力无需向 API 增加专用转发路由。
- 完成全项目构建、自动化测试、生产服务模板与能力注册表检查。
- 运行空间协调和 API 初始化任务允许访问服务账号目录，以便 Corepack 写入能力包元数据；对外运行的独立能力服务仍保持用户目录隔离与资源限制。

## 2026-08-24 09:00:00 +0800

- 平台管理的“操作记录”调整为默认收起，点击“查看记录”后展开，减少页面默认占用空间。

## 2026-08-24 09:10:00 +0800

- 更新项目发布约束：完成项目修改后自动按风险执行必要测试、提交、部署与验证；纯咨询和只读检查不触发发布流程。

## 2026-08-24 09:20:00 +0800

- 删除账号后立即触发运行空间同步：移除对应运行时定义并通知服务管理器停止旧实例，不再等待定时协调任务。

## 2026-08-24 10:00:00 +0800

- 修复员工查询会话被交互式追问工具卡住后无法继续或删除的问题：运行中的会话可终止并删除，员工查询功能改为普通文字请求补充信息，不再调用未提供交互入口的追问工具。

## 2026-08-24 10:20:00 +0800

- 员工档案录入新增工作电话、紧急联系人电话及 18 位身份证号格式校验；工作邮箱与个人邮箱明确为选填。
- 婚否、育否调整为“未填写 / 未婚或已婚 / 未育或已育”的下拉选项。

## 2026-08-24 10:30:00 +0800

- 员工档案“婚否”下拉选项新增“离异”。

## 2026-08-24 11:00:00 +0800

- 审计记录新增员工档案新增、编辑、离职与简历变更，保存操作者账号、目标员工及变更字段名称；敏感信息只记录字段发生变更，不保存具体内容。
- 审计记录改为长期保留与键集分页按需读取，平台管理页面每次加载 30 条，可继续加载全部历史记录，避免随记录增长占用页面与服务资源。

## 2026-08-24 11:20:00 +0800

- 平台管理“操作记录”新增“导出全部 CSV”，按 500 条一批从数据库流式导出全部历史审计记录，避免一次性加载造成内存压力。
- CSV 仅包含操作时间、操作者、操作对象、目标员工与变更字段；不导出敏感字段的具体内容，并对表格公式字符做安全转义。

## 2026-08-24 12:00:00 +0800

- 新增基础功能“工作助理”，位于侧边栏概览下方；所有启用账号自动获得独立 DSH 运行空间，无需单独配置功能权限。
- 工作助理第一期支持 CSV、TSV、XLS、XLSX 表格文件上传、下载、删除与 DSH 任务处理；每账号工作区按实际文件占用计算，逻辑配额为 3GB，单个上传文件限制为 200MB。
- 工作文件按账号隔离，上传与删除纳入审计；拒绝路径穿越和符号链接下载，处理规则要求保留原始文件并生成新的结果文件。

## 2026-08-24 12:20:00 +0800

- 工作助理文件范围扩展为常用表格与文档：新增 DOC、DOCX、Markdown、TXT、PDF、RTF 上传和工作区处理支持。

## 2026-08-24 12:30:00 +0800

- 修复工作助理任务提交后只读取一次会话历史、队列任务完成后页面不再刷新回复的问题；现在发送内容会立即显示，并持续检查任务状态直到回复完成或达到时限。

## 2026-08-24 12:40:00 +0800

- 工作助理回复新增安全 Markdown 呈现，支持标题、加粗、斜体、列表和行内代码；解析不写回消息状态，避免高频重渲染与节点累积。
- 新增“清空对话”：安全终止当前任务、删除该工作空间会话及其持久记录后自动创建空白会话。

## 2026-08-24 12:50:00 +0800

- 修复能力包更新后已运行 Agent 未自动重启而继续执行旧代码的问题；运行时按能力包构建产物生成版本标记，仅重启发生代码变化的实例。

## 2026-08-24 13:10:00 +0800

- 工作助理将原始上传文件、处理结果和内部临时文件分别限制在 `uploads/`、`outputs/`、`.work/` 目录；文件列表、下载与删除仅面向前两类，避免文档解析依赖和中间文件出现在工作文件中。
- 文件上传新增拖拽支持与实时进度显示；上传后的列表和配额统计只扫描用户文件目录，避免内部依赖文件拖慢页面。
- Nginx 上传上限调整为 200MB，并关闭 API 上传请求缓冲，使文件直接流式转发至服务端。

## 2026-08-24 13:40:00 +0800

- 修复工作助理发送消息后被尚未写入完成的会话历史覆盖、导致消息暂时消失的问题；现在保留待确认消息直至持久历史返回，并阻止旧会话的异步响应覆盖新会话。

## 2026-08-24 14:10:00 +0800

- 工作助理会话历史新增生成中片段呈现，不再只在任务全部结束后显示回复；处理中可手动停止，连续两分钟无任何进展时自动取消任务并恢复可发送状态，避免单个卡死会话长期占用页面。

## 2026-08-24 14:30:00 +0800

- 工作助理文件区调整为输入区与输出区：用户上传文件进入输入区，明确要求生成的结果单独进入输出区。
- 普通解读、分析、总结和问答默认仅在对话中回复；只有用户明确要求生成、导出或保存文件时才创建输出文件。

## 2026-08-24 14:50:00 +0800

- 修复工作助理未识别 DSH 压缩文本事件而在已生成回复后仍显示“正在生成”的问题；现在可持续呈现压缩后的正文片段并在最终回复到达后正确结束。
- 修复批量初始化运行空间时重启标记可能被 systemd 提前消费的竞态；能力包更新完成后统一发布完整重启清单，确保所有已运行实例加载最新处理规则。

## 2026-08-24 15:00:00 +0800

- 工作助理以最新会话轮次的完成事件结束页面等待，不再仅依赖可能短暂滞后的运行状态，避免回复已完成后仍显示“正在处理”。

## 2026-08-24 15:20:00 +0800

- 工作助理回复新增 Markdown 表格渲染，表头、单元格和横向溢出均按表格展示，不再以竖线文本显示。
- 所有 Agent 运行空间迁移为按 `Agent--账号` 独立的沙箱目录；systemd 仅授予当前实例沙箱写权限，禁止写入项目源码、运行时配置及其他账号的运行空间。

## 2026-08-24 15:30:00 +0800

- 修复 Agent 沙箱迁移脚本在生产构建中的可空路径类型检查，确保迁移在执行前完成严格校验。

## 2026-08-24 16:00:00 +0800

- 工作助理在等待处理结果期间主动从服务端历史对账；网络切换或服务重启导致完成通知遗漏时，自动补齐完整回复并结束等待，不再要求刷新页面。

## 2026-08-24 16:20:00 +0800

- 修复工作助理历史事件的压缩序号排序与完成态识别：完整回复事件即视为本轮结束，即使结束通知延迟或缺失也会自动替换半截内容并结束“正在生成”。

## 2026-08-24 16:40:00 +0800

- 工作助理完成态改为按会话轮次判断，兼容长回复历史窗口截掉用户消息的情况；请求链中断后只要仍有流式片段便持续自动对账。
- 工作助理明确选择当前账号的沙箱工作空间，避免运行空间迁移后的旧工作空间记录干扰会话恢复。

## 2026-08-24 17:00:00 +0800

- 工作助理在确认工作空间和会话后立即结束连接状态，历史与文件列表改为后台加载并增加请求超时，避免辅助数据请求悬挂整个界面。
- 处理中仅同步最近两轮历史并合并到已有对话，不再每 2.5 秒重复下载完整会话事件，降低长对话的网络、解析和渲染负担。

## 2026-08-24 17:30:00 +0800

- 重构工作助理会话链路为明确的连接状态与任务状态控制器：通过实时事件流增量呈现回复和确认结束，移除提交等待、定时全量轮询和局部恢复之间的状态竞争。
- 网络中断后自动重连并仅同步最近两轮补齐漏失事件；压缩历史与实时片段合并时按事件范围去重，避免重复正文、半截回复及错误的“正在生成”。
- 工作空间文件上传、删除、配额和错误状态拆分为独立文件控制器；连接确认不再等待历史或文件列表，清空、停止、超时及请求取消均由统一会话控制器收口。

## 2026-08-24 17:50:00 +0800

- 平台聊天输入框统一增加输入法组合状态判断；中文、日文等输入法使用回车确认候选词时不发送消息，组合结束后再次按回车才提交，Shift+Enter 继续换行。

## 2026-08-24 18:10:00 +0800

- 修复工作助理消息提交后因临时消息与实时历史合并竞态而短暂消失的问题，正式用户消息写入界面前持续保留待确认消息。
- 禁止工作助理调用网页无法承接的交互式提问工具；已有会话若停在此类调用，连接后自动清理损坏会话并重建可用会话，避免后续消息只进入队列而无响应。

## 2026-08-24 18:30:00 +0800

- 更新工作助理回复约束：保持专业办公表达、拒绝无关闲聊，并禁止在回复中使用 Emoji 或颜文字。

## 2026-08-24 18:45:00 +0800

- 修复工作助理等待回复时对话窗口抖动：对话区改为稳定的响应式高度并固定滚动条占位，发送与停止按钮保持相同宽度，避免流式内容和状态切换引发布局位移。

## 2026-08-24 19:00:00 +0800

- 调整平台审计范围：工作助理中的文件上传、删除及其他工作区操作不再写入操作记录；审计列表与 CSV 导出同步排除已有工作助理记录。

## 2026-08-25 09:20:00 +0800

- 修复新账号 Agent 进程已启动但工作区注册为空导致前端持续重连的问题：通用运行器在启动后校验并幂等补建当前实例工作区，覆盖员工查询、工作助理及后续注册的 Agent。
- Agent 健康检查由端口探测升级为工作区 API 深度检查；进程在线但工作区缺失时不再误报健康。

## 2026-08-25 09:40:00 +0800

- 修复新账号配置发布早于 Agent 能力包安装导致基础 Coding Agent 抢先启动的竞态：运行器必须等待已安装能力包版本与当前代码完全一致后才启动 DSH。
- 工作区改为业务插件完成提示词、工具和路由注册后的最终就绪标记，并增加 Agent 与账号身份就绪接口；缺少业务能力、版本不一致或身份不匹配时不再由通用运行器代建工作区并误报健康。该约束由统一 Agent 清单驱动，适用于后续新增账号和 Agent。

## 2026-08-25 14:40:00 +0800

- Agent 运行时改为首次访问自动唤醒，同一实例的并发访问共享启动过程；页面保持连接或请求执行期间持续记录活动，避免正常使用时被回收。
- 运行时空闲 30 分钟后由 systemd 协调任务停止并释放内存，运行空间、会话和文件继续保留；再次访问时自动恢复。
- 健康检查区分“运行中、待命、不可用”，待命实例不再被误报告警；新账号和后续注册 Agent 继续沿用统一能力包准备、身份校验和按需启动机制。

## 2026-08-26 11:30:00 +0800

- 修复员工查询完成回复只能刷新后显示的问题：实时事件与持久化历史改为按事件序号合并，任务完成、运行状态结束、连接恢复及处理期间均主动对账；对账以本轮发送前的事件序号为边界，避免把上一轮回复误判为本轮完成，并兼容压缩文本事件。
- 收紧工作助理内部配置边界：禁止披露、复述或变相输出系统提示、职责约束、工具配置和安全规则；相关询问统一返回不包含内部细节的说明。

## 2026-08-26 11:42:00 +0800

- 将现有业务功能和真实数据接入管理驾驶舱：新增在职/离职与员工状态指标、一级部门分布、7天合同到期提醒、账号状态、运行空间健康、模块启停和近期操作记录，并提供员工档案、员工查询、工作文件与平台管理快捷入口。
- 新增驾驶舱专用聚合接口与员工统计仓储查询；接口仅允许拥有驾驶舱权限的 CEO 使用，复用员工、账号、平台状态和审计数据源，不生成财务、项目等尚未建设的数据。

## 2026-08-26 11:55:00 +0800

- 在员工管理下新增“考勤与汇报”独立模块，提供按日期查看日报提交、动态汇报内容、上下班打卡及异常情况的页面；企业微信未联通期间使用明确标注的演示数据。
- 后端建立可替换的考勤与汇报数据源边界，保留企业微信成员标识和动态表单结构；正式接入时可替换数据源而不改前端接口与驾驶舱。
- 新增独立“考勤与汇报”账号权限、平台模块启停能力和已有员工档案账号的权限迁移，并将当日日报、考勤摘要接入管理驾驶舱。
- 补充企业微信汇报、打卡数据形式和正式接入配置说明，区分备案域名与服务器企业可信 IP 的用途。

## 2026-08-26 12:20:00 +0800

- 将“考勤与汇报”拆分为“考勤管理”和“工作汇报”两个员工管理模块，分别提供独立导航地址、账号权限、后端接口和业务页面。
- 原“考勤与汇报”权限自动迁移为两个新权限，既有账号升级后不会丢失入口；企业微信数据适配和人员映射继续在员工模块内共享，避免重复同步。
- 修正历史权限迁移在重复执行时拒绝新权限的问题，并增加迁移兼容性测试，确保已有数据库和后续重复部署均可完成权限升级。

## 2026-08-27 10:30:00 +0800

- 新增独立“会议管理”模块和“查看全部会议记录”权限，以横向会议列表展示编号、标题、开始时间、模式和参会人员，按需查看 Markdown 摘要与原文。
- 建立与员工档案隔离的会议数据表和只读仓储；会议编号按年份与年度流水自动生成，网页端不提供编辑或删除能力，并为未来会议内容问答保留独立数据边界。
- 新增会议记录上传接口，支持专用 Bearer 凭证、幂等重试、中文/双语模式、可空摘要、带时区起止时间和参会人员校验；列表分页不加载大段正文。
- 平台管理新增会议上传凭证的生成与轮换入口，凭证仅完整显示一次、数据库仅保存摘要值，并记录最后使用时间。

## 2026-08-27 11:45:00 +0800

- 会议上传凭证支持创建时命名、同时管理多个凭证并逐个删除，删除后对应翻译脚本立即失去上传能力。
- 修复会议凭证复制按钮：安全上下文优先使用剪贴板接口，不可用时回退到兼容复制方式，并在界面反馈复制结果。
- 调整会议搜索栏弹性布局与输入框最小宽度，避免“搜索编号、标题或参会人员”末尾文字被裁切。

## 2026-08-27 12:20:00 +0800

- 优化平台管理账号列表的权限展示，将过长权限文本收敛为紧凑标签与剩余数量，避免挤压表格布局，同时保留完整权限悬停说明。
- 优化会议上传凭证创建流程：点击生成后再填写凭证名称，并缩小凭证列表中的名称字号。

## 2026-08-27 12:35:00 +0800

- 修复账号权限摘要无法查看完整内容的问题：悬停或键盘聚焦时以不受表格裁切的浮层展示全部权限；同时缩小会议上传凭证的删除操作字号。

## 2026-08-27 12:45:00 +0800

- 统一平台管理中的删除操作样式：账号删除与会议凭证删除使用相同的字号、图标、描边、圆角、间距及悬停反馈，并提取共享样式供后续删除入口复用。

## 2026-08-27 13:00:00 +0800

- 为平台管理的模块列表增加展开与收起功能，并强化账号及会议凭证删除按钮的统一描边方框，提升操作区辨识度。

## 2026-08-27 13:15:00 +0800

- 优化平台模块管理布局：将单列超长模块条调整为响应式多列短条，收紧模块名称、状态与启停按钮的间距，并在窄屏下自动恢复单列展示。

## 2026-08-28 09:30:00 +0800

- 修复会议摘要渲染能力不足的问题：会议模块新增安全的结构化 Markdown 解析，正确展示标题、段落、加粗、斜体、删除线、链接、列表、引用、代码块、分隔线和表格，并兼容上传端二次转义换行。
- 为会议摘要解析补充格式覆盖与未闭合代码块回归测试，避免异常内容造成渲染停滞。

## 2026-08-28 10:05:00 +0800

- 统一侧边栏普通模块、模块分组与分组内功能的高度和垂直间距，统一图标占位并保留清晰的子功能缩进；移除全部导航链接下划线及分组分隔线。

## 2026-09-01 10:40:00 +0800

- 在登录页和登录后的平台页面底部展示“鄂ICP备2023016886号-2”，并链接至工业和信息化部备案系统。

## 2026-09-01 11:10:00 +0800

- 会议管理的摘要与原文查看页增加 Word 导出，输出真实 `.docx` 文件，包含会议信息、标题、列表、表格、引用和代码等结构化排版。
- 导出逻辑独立于页面渲染，并增加文件名清理、导出状态及失败提示，避免与会议弹窗样式冲突。

## 2026-09-01 14:30:00 +0800

- 在员工工作汇报边界内新增企业微信智能表格日报持久化，以 `record_id` 幂等写入，保存日报正文、人员部门、时间、附件元数据和原始字段。
- 新增历史 NDJSON 导入和 `wecom-cli` 游标自动分页同步，通过内容哈希识别新增、更新和未变数据，不使用未验证的时间过滤参数。
- 建立同步运行日志、并发锁、手动命令和每 5 分钟后台同步服务模板；企业微信凭证继续由 `wecom-cli` 在独立服务目录中加密保存，不写入项目配置或日志。
- 同步客户端直接解析 `wecom-cli` 的完整 JSON 响应，兼容其多行缩进输出，分页仍由经验证的 `next_cursor` 驱动。
- 企业微信日报定时同步调整为每天 22:00 执行；分页扫描后仅写入新增或内容发生变化的记录。

## 2026-09-01 14:55:00 +0800

- 在员工工作汇报模块内新增日报统一查询服务，支持员工、部门、日期范围、正文关键词、分页和单条详情查询。
- 新增受“工作汇报”权限与模块启停控制的日报 API；对外仅返回统一字段，不暴露同步原始数据和数据库连接。
- 补充部门、员工姓名查询索引及服务回归测试。

## 2026-09-01 15:05:00 +0800

- 将员工模块原“工作汇报”入口升级为“日报管理”，接入已入库的企业微信日报，支持日期范围、部门、员工和正文关键词筛选。
- 新增日报分页列表与只读详情抽屉，完整展示工作总结、明日计划、其他事项和附件查看入口，并补齐加载、空数据、无筛选结果和请求失败状态。
- 日报页面复用 `employee-reports` 权限和现有模块注册，统一侧边栏、驾驶舱、账号权限和平台模块中的功能名称。

## 2026-09-01 15:23:00 +0800

- 日报管理页面新增手动同步入口，展示等待、运行、完成、部分成功和失败状态，完成后自动刷新当前日报列表。
- 新增受“日报管理”权限保护的手动同步触发与状态接口，记录发起账号，且不在 API 请求中执行长时间同步。
- 新增 systemd path 触发单元，手动与每天 22:00 自动同步共用原有受限服务、数据库并发锁、运行日志和故障告警。

## 2026-09-01 15:50:00 +0800

- 修复 PostgreSQL `date` 字段在 API 序列化时被 UTC 转换导致汇报日期整体提前一天的问题，日报日期现按中国标准时间准确返回。
- 当实际提交日期晚于汇报日期时，日报列表和详情统一标记为“延后提交”。

## 2026-09-01 16:42:00 +0800

- 日报管理新增提交看板，统一计算工作日应提交、已提交、未提交与延后提交人数，支持按指标和部门下钻查看人员，并可导出未提交、延后提交名单。
- 新增月度日历、员工日报档案和部门汇总，支持按日期、周月范围查看提交状态、历史日报、连续提交、常用工作内容、完成事项、计划、问题及提交异常人员。
- 新增周报、月报结构化汇总草稿及人工确认流程，汇总内容不用于员工评价；新增部门汇总 Excel 导出。
- 新增日报数据检查，识别同人同日重复、汇报日期晚于提交日期、人员或部门缺失、内容为空以及企业微信成员未关联员工档案。

## 2026-09-01 17:00:00 +0800

- 从日报管理中移除“部门汇总”和“周月汇总”入口、接口、汇总生成确认逻辑及新环境的数据表迁移。
- 生产环境已有的一条汇总草稿仅作为不可访问的历史归档保留，未删除业务数据；提交看板中的部门完成情况继续保留。

## 2026-09-01 17:20:00 +0800

- 日报填写人新增企业微信成员 ID 与员工档案的稳定关联，仅对姓名唯一且成员 ID 唯一的记录自动建立关联，避免同名或账号异常时错误匹配。
- 日报列表与详情改为展示员工档案中的当前一级、二级部门，并保留企业微信提交时部门作为历史信息；未能安全关联的填写人会明确标记。
- 员工和部门筛选同时支持企业微信原始字段与员工档案当前字段，解决两套部门口径导致的展示和查询不一致。

## 2026-09-02 09:20:00 +0800

- 员工档案详情增加历史日报，按日期从新到旧仅展示汇报日期和工作内容。
- 历史日报通过员工档案与企业微信成员的稳定关联读取，每页加载 20 条，并区分未关联与暂无日报状态。

## 2026-09-02 10:05:00 +0800

- 优化网页生产构建中的图标导入，构建时自动转换为具体图标文件，避免每次扫描整套图标模块。

## 2026-09-02 10:25:00 +0800

- 日报管理的员工档案取消默认本月日期限制，统计和详情改为读取已同步的全部历史日报。
- 员工历史日报详情按每批 100 条分页读取并合并展示，避免历史数据超过单次接口上限时被截断。

## 2026-09-02 10:35:00 +0800

- 员工日报档案中的历史日报取消单行省略，完整保留段落换行并自动多行展示全部工作内容。

## 2026-09-02 10:50:00 +0800

- 员工日报档案增加姓名搜索，在已加载档案中即时筛选匹配员工。
- 提交看板默认统计当日前一日，并增加前一天、下一天快速切换。

## 2026-09-02 11:05:00 +0800

- 会议记录列表将开始时间、会议模式与参会人员改为稳定网格布局，参会人员多行换行时不再影响前两项对齐。
- 窄屏下参会人员独占下一行，保持时间、模式和操作按钮排版稳定。

## 2026-09-02 11:20:00 +0800

- 账号管理移除停用与启用操作及其公开接口，账号列表仅保留编辑、必要的初始化重试和删除操作。
- 重置密码操作从账号列表转移至编辑账号界面，新增账号时不显示该操作。

## 2026-09-02 11:35:00 +0800

- 员工日报档案移除“常用工作内容”区域，详情弹窗直接展示全部历史日报。
- 后端同步停止汇总、切分和返回工作内容频率字段，减少员工档案查询的无用聚合。

## 2026-09-02 11:50:00 +0800

- 前端新增统一的 Skeleton / Shimmer 占位组件，提供卡片、表格、列表、头像、文字和详情结构。
- 平台恢复、员工信息、组织架构、日报、考勤、会议、管理驾驶舱及平台管理的数据加载状态改为低对比度结构化占位，减少内容替换时的布局跳动。
- 保存、提交、同步与导出等短时操作继续使用按钮内进度反馈，不改变原有业务和数据请求逻辑。

## 2026-09-03 10:14:24 +0800

- 新增企业微信打卡数据同步：按 employees.wecom_user_id 关联员工，支持历史补拉、增量同步、30 天时间窗口与每批 100 名员工。
- 新增打卡原始数据、同步 checkpoint 与运行日志存储；同步仅在全部成功时推进 checkpoint，并通过稳定记录键保证重复拉取幂等。
- 新增每日 Asia/Shanghai 定时同步模板及最近 7 天、指定日期范围、指定企业微信用户的手动命令。

## 2026-09-03 10:24:31 +0800

- 考勤管理页面接入已同步的企业微信打卡数据，页面读取 PostgreSQL 标准化结果，不再显示演示数据。
- 按员工汇总上下班打卡、企业微信返回的计划打卡时间、异常状态与地点；指标改为已记录员工、正常打卡和企业微信标记异常，不新增考勤规则判断。

## 2026-09-03 10:35:47 +0800

- 完成 2026-07-01 起的企业微信打卡历史补拉；考勤页面增加前后日期切换、姓名/部门/状态筛选、异常优先排序和当前筛选结果 Excel 导出。
- 新增单人当日打卡详情，展示企业微信返回的上下班时间、计划时间、异常说明与地点；不展示设备、Wi-Fi、精确位置等敏感字段，且不显示班次信息。

## 2026-09-03 10:40:29 +0800

- 移除考勤页面的企业微信同步提示条，收紧页面顶部空间。
- 日期前后切换改为不受浏览器时区影响的日历计算，确保“后一天”可稳定切换查询日期。

## 2026-09-03 11:19:25 +0800

- 新增企业微信通讯录关联：仅在员工姓名与企业微信成员姓名双方唯一时自动写入企业微信账号 ID，不以企业微信部门覆盖员工档案部门。
- 员工档案页新增“关联企业微信”操作，并记录关联结果；无法唯一确认的员工保持未关联，供后续人工处理。

## 2026-09-03 13:33:35 +0800

- 修复日报管理“员工档案”将离职人员纳入卡片列表的问题，档案视图现在仅展示在职员工。
- 复核日报模块其余人员范围：提交看板和日历已按在职日期计算；日报列表、历史日报和数据检查保留历史离职记录，符合其查询用途。

## 2026-09-03 13:43:15 +0800

- 新增企业微信排班同步与排班存储，日报看板、日历和考勤管理按企业微信实际排班判断应出勤日期；休息排班不再计入缺卡或未提交。
- 考勤管理列表移除计划上班、计划下班两列，仅保留实际打卡数据与企业微信状态。

## 2026-09-03 13:50:00 +0800

- 日报管理新增单独汇报名单，并从每日应提交统计中排除已关联人员；提交看板新增请假、未排班及单独汇报说明卡片。

## 2026-09-03 14:09:44 +0800

- 将日报管理“单独汇报”标签移动至“数据检查”之后。

## 2026-09-03 14:12:08 +0800

- 移除日报提交看板中的“请假、未排班”统计卡片。

## 2026-09-03 14:18:25 +0800

- 点击日报“当日应提交”卡片时，先展示请假、未排班人员，再展示实际应提交名单。

## 2026-09-03 14:28:37 +0800

- 优化日报“当日应提交”详情，以人数摘要、请假与未排班说明、应提交名单三层结构展示统计内容。

## 2026-09-03 15:16:00 +0800

- 历史考勤改为按查询日期与员工入职、离职日期判断，离职后仍保留并可查看在职期间的历史记录。
- 日报员工档案增加“在职员工 / 离职归档”切换，默认仅展示在职员工，离职员工的历史日报独立归档保留。

## 2026-09-03 15:24:54 +0800

- 员工数据维护的离职员工编辑表单增加可编辑的离职日期，并校验离职日期不得早于入职日期。

## 2026-09-03 15:54:27 +0800

- 日报管理的员工档案统计调整为提交天数、延后天数和未交天数；未交天数按员工实际排班、任职日期及已提交日报计算。

## 2026-09-03 16:00:00 +0800

- 将员工日报档案的未交天数统计起点设为 2026 年 8 月 8 日，更早的排班不计入未交。

## 2026-09-03 16:17:18 +0800

- 日报列表、员工日报档案及相关统计统一排除“单独汇报”人员。
- 企业微信汇报日期晚于实际提交日期时，按实际提交日期展示和统计；正常的延后提交仍保留原汇报日期。

## 2026-09-04 09:33:43 +0800

- 考勤管理增加单个员工全部出勤记录查看功能，并增加当月打卡异常排行与异常统计。
- 上下班打卡任意一项缺失即标记为缺卡，缺失的实际时间保持为空；9:01–9:15 标记黄色迟到，9:16 起标记红色迟到。
- 考勤概览的异常指标统一使用“打卡异常”业务文案。

## 2026-09-04 09:57:17 +0800

- 考勤日期默认为前一天，不查询、展示或统计当天数据，并取消当日详情入口。
- 考勤按企业微信每日实际排班识别大小周、节假日与周六补班，仅对当日有班次的员工判定缺卡。
- 增加企业微信已审批请假时段同步；整天、8 小时或覆盖上下班时点的请假显示为“请假”，不计入打卡异常。
- 考勤 Excel 与页面一致取消计划上下班字段，并支持请假状态导出。

## 2026-09-04 10:30:01 +0800

- 考勤页面增加应出勤、已出勤、迟到、缺卡和请假概览，并增加状态筛选与“仅看异常”快捷筛选。
- 考勤表格精简为姓名、部门、上下班时间、状态和地点，并可通过员工姓名查看个人当月统计与全部历史记录。
- 考勤状态采用绿色、橙色、红色和灰蓝色区分正常、迟到、缺卡和请假，优先突出异常记录。

## 2026-09-04 10:45:03 +0800

- 考勤页面的应出勤、已出勤、迟到、缺卡和请假概览卡片增加人员名单交互，并可从名单直接进入员工个人考勤详情。

## 2026-09-04 10:52:55 +0800

- 考勤管理增加可切换月份的月度统计，展示应出勤、已出勤、迟到、缺卡人次、出勤率及上月对比。
- 增加部门考勤汇总和月度异常前 10 名排行，部门按缺卡、迟到情况优先排列。
- 月度数据由后端单次聚合返回，避免前端按日重复请求。

## 2026-09-04 10:57:06 +0800

- 考勤管理增加“每日考勤 / 月度考勤”标签切换，月度统计、部门考勤和异常排行移入独立月度页面，不再堆叠在每日明细下方。

## 2026-09-04 11:14:58 +0800

- 完善企业微信请假历史与增量同步，按审批编号幂等保存全部审批状态，并记录同步检查点、运行结果和失败信息。
- 请假只通过员工档案中的企业微信 userid 关联；考勤仅使用已通过且覆盖当天的请假记录，保留原始打卡数据不变。
- 增加员工单日状态接口，统一返回其考勤、请假和日报关联结果；既有考勤定时同步在配置审批凭证后自动同步请假。

## 2026-09-04 11:29:00 +0800

- 修正企业微信审批列表分页参数，按接口要求使用数字型 cursor，并兼容分页游标返回字段。

## 2026-09-04 11:51:26 +0800

- 拆分企业微信通讯录账号 ID 与日报填写人 ID，考勤、排班、请假和日报按各自稳定标识关联，避免数据源 ID 相互覆盖。
- 通讯录同步由仅补充空白关联升级为全量校验，可自动修正姓名唯一员工的错误或过期账号 ID；每日考勤同步前自动执行校验。
- 保留既有日报关联并补充已确认的外籍员工通讯录映射，为请假历史重新关联提供基础。

## 2026-09-04 12:01:00 +0800

- 补充从既有排班与打卡归属恢复日报填写人 ID 的迁移逻辑，兼容日报姓名与员工档案姓名不一致的历史记录。

## 2026-09-04 12:12:00 +0800

- 修正企业微信审批分页提前结束问题：自建应用返回不足 100 条时仍按 next_cursor 继续拉取，避免遗漏后续请假审批。

## 2026-09-04 12:25:00 +0800

- 增加企业微信审批空游标与重复游标保护，避免结束页返回空值或游标回绕时重复拉取。

## 2026-09-04 13:28:25 +0800

- 考勤聚合不再将企业微信“未打卡”占位记录中的计划时间显示为实际上下班时间。
- 外出打卡作为辅助明细保留，不替代上下班打卡；地点按地点名称、详细地址、坐标依次回退展示。

## 2026-09-04 14:04:23 +0800

- 在新增员工窗口增加“智能识别”粘贴面板，按固定 30 列解析 Excel 单行数据并填入员工表单。
- 增加列数、日期、试用期及计算字段交叉校验，年龄、工龄和合同到期值仅用于校验，最终由现有新增员工保存流程写入数据库。

## 2026-09-04 14:10:43 +0800

- 员工信息粘贴识别时，将用工类型“合同工”统一映射为“全职”。

## 2026-09-04 14:42:07 +0800

- 修复历史员工导入后员工编号序列未同步，导致新增员工触发重复唯一字段的问题。
- 新增员工时自动将编号序列校准到现有最大编号，并通过事务锁避免并发分配冲突。

## 2026-09-04 15:12:17 +0800

- 员工信息粘贴识别模板由 30 列更新为固定 33 列，增加婚否、育否和籍贯字段，并同步调整后续字段映射位置。

## 2026-09-04 15:49:56 +0800

- 修复员工入职或离职后局部列表状态与旧分页缓存混用导致的页面错乱，操作成功后统一清理缓存并重新加载当前列表。
- 入职、离职和档案保存成功后增加轻量级状态提示动画，并支持系统减少动画偏好。

## 2026-09-04 16:50:49 +0800

- 日报提交看板改为按查询日期读取企业微信已通过的请假记录，并与考勤统计共用员工关联和日期覆盖口径。
- 日历视图同步排除当日请假人员，避免看板与日历的应提交人数不一致。
- 日报同步状态禁用 HTTP 缓存，并在页面重新可见、窗口重新聚焦及每分钟自动更新。
- 修复日报同步任务主键转为文本后按字符串排序，导致任务编号达到两位数后始终返回第 9 次旧同步记录的问题。

## 2026-09-04 17:56:37 +0800

- 删除侧边栏左下角的“内部开发阶段”和“按账号已开通功能显示”说明。

## 2026-09-05 09:12:51 +0800

- 员工日报档案取消提交、未交和延后天数统计展示，改为简洁的员工姓名与部门条目，点击后查看完整历史日报。

## 2026-09-05 09:48:45 +0800

- 新增独立“招聘管理”模块及权限，支持手动新建岗位、批量上传 PDF/DOC/DOCX 简历、按岗位条件生成优先查看/人工复核/明显不匹配三档结果。
- 简历、解析文本与筛选结果独立保存；支持单份、批量与一键确认淘汰，可恢复为人工复核，永久删除单独提供。
- PDF 与 DOCX 进行本地文本提取后参与筛选；无法解析的简历保留原件并转为人工复核，不自动淘汰。
- 同步扩展账号功能权限约束，允许平台分配“招聘管理”权限。
- 现有人事经理、人事专员、CEO 和平台管理员自动获得招聘管理访问权限；仍可在账号管理中为其他人单独分配或移除。
- 更新可重复执行的历史权限迁移约束，保证后续数据库迁移不会因新增招聘权限而失败。
- 补齐所有历史账号权限迁移中的招聘权限约束，保证全量重复迁移时也保持兼容。
- 招聘简历上传改为逐份提交并限制单份 5 MB，避免大量文件在同一请求中超过服务请求体限制而上传失败。

## 2026-09-05 10:10:00 +0800

- 侧边栏新增与“当前功能”同级的“待开发”分组，将财务管理和项目管理移入该分组；模块权限、路由和业务功能保持不变。

## 2026-09-05 10:15:00 +0800

- 优化侧边栏一级模块对齐与二级菜单展开样式：二级内容采用浅色分组容器、统一缩进和层级引导线，明确父子归属并拉开模块间距。

## 2026-09-05 10:25:00 +0800

- 侧边栏移除二级菜单与标题的层级连线，并降低展开分组容器的视觉重量。
- 招聘管理新增岗位要求查看与岗位级删除能力；删除岗位会同时删除其简历记录并要求二次确认。
- 优化招聘岗位详情区的分组间距、标题和空状态对齐，统一三档筛选结果的呈现结构。

## 2026-09-05 10:45:00 +0800

- 招聘管理统一为平台绿色视觉；新增候选标记和独立候选名单视图，支持待联系、已约面、面试中、待录用、已录用、不考虑等阶段及跟进备注。
- 候选状态和备注独立持久化，支持从候选池撤回；所有阶段变更写入平台审计记录。

## 2026-09-05 10:55:00 +0800

- 招聘简历列表移除未选择岗位提示标签，文件名改为中性深色；固定右侧候选、淘汰和删除操作区，筛选说明可自动换行且“请人工核对岗位职责相关性”单独展示。

## 2026-09-05 11:05:00 +0800

- 岗位简历列表仅显示待处理简历；候选和淘汰后分别移入候选名单与淘汰名单，淘汰名单支持恢复到原岗位。
- 恢复未选择岗位提示文字并隐藏图标，调整文件名层级与右侧操作对齐。

## 2026-09-05 11:15:00 +0800

- 修复侧边栏展开模块时内容撑高页面、触发滚动条变化造成的页面跳动：侧边栏导航改为独立滚动区域，并稳定保留页面滚动条空间。

## 2026-09-05 11:25:00 +0800

- 淘汰名单增加为招聘管理顶部与候选名单同级的独立入口，点击后直接定位到独立淘汰名单区。

## 2026-09-05 11:35:00 +0800

- 修复淘汰名单入口错误显示候选名单的问题，改为独立视图切换；恢复操作增加失败提示并统一为绿色操作按钮样式。

## 2026-09-05 11:45:00 +0800

- 修复招聘简历淘汰/恢复 SQL 参数类型冲突，恢复按钮现可正常将简历返回原岗位。

## 2026-09-05 11:55:00 +0800

- 修复招聘顶部导航在切换淘汰名单后未显示选中态的问题，候选名单与淘汰名单分别维护绿色选中效果。

## 2026-09-05 12:05:00 +0800

- 新增全平台复用分页组件，支持上一页、页码、省略号和尾页；招聘淘汰操作取消浏览器二次确认。

## 2026-09-05 12:15:00 +0800

- 调整在职员工表格中在职状态与用工类型的纵向排版，使两行内容左侧统一对齐。

## 2026-09-05 12:32:00 +0800

- 修复考勤管理未将企业微信外出打卡纳入上下班判断而反复误判缺卡的问题；保留外出地点，并对单次外出打卡谨慎保留另一时段的缺卡判断。

## 2026-09-05 13:05:00 +0800

- 升级审计记录：员工档案新增、编辑、离职及简历变更开始保存字段级修改前后值；身份证、电话、银行卡、住址与邮箱按规则脱敏，操作记录支持展开查看与 CSV 导出变更明细。

## 2026-09-05 13:28:00 +0800

- 扩展统一审计明细至会议上传、招聘岗位与简历、候选阶段、账号和模块管理；会议上传由系统身份记录，新增功能应复用同一结构化变更格式。

## 2026-09-05 14:08:00 +0800

- 补齐招聘简历淘汰与恢复操作的审计变更明细，记录涉及的简历文件、处理数量与目标状态。

## 2026-09-05 14:35:00 +0800

- 在日报管理中新增临时汇总分析与范围问答入口，后端按日报管理权限读取指定范围日报并调用专用外部模型配置；结果不写入数据库。

- 汇总分析首次打开时默认选择昨日，仍支持调整自定义日期范围。

## 2026-09-05 14:50:00 +0800

- 优化日报汇总分析界面为与平台一致的企业蓝分析工作区，并安全渲染模型返回的 Markdown 标题、列表、段落与加粗内容。

## 2026-09-05 14:58:00 +0800

- 依据《和工作平台设计规范》将日报汇总分析调整为玉青白企业管理风格，移除星光类图标与蓝色主视觉。

## 2026-09-05 15:10:00 +0800

- 重整日报管理“汇总分析”界面：明确生成汇总与按问题查询两类操作层级，统一玉青主按钮、描边次按钮及结果区样式，改善移动端排版。

## 2026-09-05 15:20:00 +0800

- 修复招聘管理切换淘汰名单时先渲染候选名单再延迟切换造成的页面抖动，改为由页面状态直接驱动名单内容。

## 2026-09-05 15:30:00 +0800

- 细化日报汇总分析的内容栅格，标题与说明文字对齐操作卡和查询卡内容边界，并移除重复模块标识。

## 2026-09-05 15:40:00 +0800

- 重构日报汇总分析为独立的“生成日报汇总”和“智能查询”功能卡，新增日期快捷范围和可点击查询示例，压缩筛选留白且不改变现有请求逻辑。

## 2026-09-05 15:50:00 +0800

- 将日报汇总与智能查询拆分为独立加载、错误和结果状态；汇总服务若首次返回空正文会自动以更短资料重试一次，避免查询操作互相覆盖或联动加载。

## 2026-09-05 16:05:00 +0800

- 修复默认日报汇总未生成正文：外部汇总请求显式使用非推理模式，避免内部推理耗尽输出预算而返回空正文。

## 2026-09-05 16:20:00 +0800

- 日报汇总与查询结果改为固定标题和简洁子弹点结构；来源标记支持点击打开对应原始日报详情，页面仅显示姓名与日期而不展示内部日报编号。

## 2026-09-05 16:35:00 +0800

- 将在职员工历史日报查看整合到日报列表的员工姓名；原员工档案入口调整为离职归档，并仅保留存在日报记录的离职员工。

## 2026-09-05 16:45:00 +0800

- 优化日报汇总输出约束：按固定栏目提炼有限关键要点、合并重复事项并限制代表性来源数量，要求完整收尾，避免逐条罗列导致的半句截断。

## 2026-09-05 16:55:00 +0800

- 日报生成汇总改为单份按部门分段的报告，覆盖全部有日报部门；适当放宽篇幅与输出额度，同时保留每部门重点条数限制以避免截断。

## 2026-09-05 17:05:00 +0800

- 日报部门汇总改用员工档案中的真实一级/二级部门路径，并将当次部门清单限定为唯一可用标题；强制每个部门下以 Markdown 子弹点输出，禁止虚构部门。

## 2026-09-05 17:15:00 +0800

- 优化日报部门汇总层级：一级、二级部门改用 Markdown 标题，只有具体事务使用子弹点；取消固定条数与来源数量，按事项相关性合并并按实际依据引用来源。
