# 和工作项目执行约束

## 严格遵守用户命令范围

1. 只执行用户当前命令明确要求的事项，以及完成该事项不可缺少的最小操作。
2. 绝不把“写入”“修改”“查看”等单一命令自行扩展为构建、测试、验证、重启、部署、重构、补充文档或其他操作；但“自动测试、提交与部署”约束另有明确规定的除外。
3. 未经用户明确要求，不修改命令范围之外的文件、配置、代码、依赖、进程或外部服务。
4. 如果命令存在歧义，或必须扩大操作范围才能继续，先停止并向用户说明，获得明确许可后再操作。
5. 不以“最佳实践”“顺便完善”“确保可用”或此前任务习惯为理由擅自扩大范围。
6. 完成后只报告实际执行的内容，不继续进行未被要求的后续工作。

## 模块化开发约束

1. 后续所有新功能和功能修改必须按模块化方式实现，先明确功能所属模块，不得将业务逻辑随意写入全局入口、平台壳层或其他无关模块。
2. 平台级功能应在 `apps/web/src/modules/<主模块>/<功能/>` 中使用独立目录，并通过 `apps/web/src/app/module-registry.ts` 统一注册；导航、页面入口和角色权限不得在多处重复硬编码。
3. 每个模块应将界面、状态管理、数据访问、业务规则和外部集成按职责拆分；不得持续向单个大型组件或单个服务文件堆叠无关功能。
4. Agent 能力、工具、数据仓储、会话管理和第三方框架适配应保持独立边界，通过明确接口组合，不得把某项 Agent 能力直接耦合进平台公共壳层。
5. 跨模块复用的类型、组件或数据规则应提取到明确的共享边界；模块不得依赖另一模块的内部实现细节，也不得复制同一份业务逻辑形成多个数据源。
6. 新增、删除或替换某个模块时，应将影响限定在该模块及其注册配置内，不应破坏其他模块。
7. 如果用户命令与上述模块边界冲突，或实现必须引入跨模块强耦合，先停止并说明原因，取得用户明确确认后再继续。

## 前端去 AI 风约束

1. 面向使用者统一采用中性、清晰的办公平台语言；不展示 AI、Agent、DSH、模型、数据库等实现或营销术语。
2. 不使用“专属”“最高权限”“随时可以提问”等身份宣传、拟人化助手或冗余状态文案；仅保留完成任务所需的角色、状态和操作信息。
3. 避免机器人、闪光等 AI 暗示性视觉元素；功能以业务名称和实际用途呈现。

## 持续适用的已确认要求

- 项目发生修改时更新 `DEVELOPMENT_LOG.md`，记录日期、时间和修改内容。
- 开发日志、文档和输出中不得记录或回显 API Key、密码等秘密值。

## 版本发布约束

1. 发布版本号采用 `x.xx` 格式：小变动递增末两位（如 `v1.02` → `v1.03`），中等变动递增小数点后一位并将末位归零（如 `v1.03` → `v1.10`），大变动递增整数位并重置小数部分（如 `v1.10` → `v2.00`）。
2. 每次发布必须在 Git 提交信息中包含版本号及本次改动摘要，并同步部署至服务器后验证网站更新。
3. 每次完成并修改项目文件的任务后，均自动执行与改动风险相称的必要测试，避免重复执行与本次改动无关的冗余检查；随后创建符合版本号规则的 Git 提交、推送并部署到服务器，完成网站或服务健康验证。
4. 纯咨询、方案讨论、只读检查或未产生项目文件修改的任务，不触发测试、提交与部署。
