# 开发日志

本文件记录“和工作”的每次项目更新。时间均使用中国标准时间（Asia/Shanghai）。

## 2026-08-14 17:11:47 +0800

- 将 GitHub 仓库 `Aoppp/DSH_HeGongZuo` 克隆到本地项目目录，并确认 `main` 分支跟踪 `origin/main`。
- 查询并确认 DSH 当前发布版本，选择精确锁定 `@deepseek-ai/dsh@0.1.0-rc.6`，避免预发布阶段的非预期升级。
- 锁定运行环境为 Node.js `>=22.19.0`、pnpm `11.7.0`，建立根 `package.json` 与 pnpm workspace。
- 添加 DSH CLI、DSH Web、配置查看和环境验证命令。
- 添加 `.env.example` 和 `.gitignore`，确保真实 API 密钥与本地运行文件不会进入 Git。
- 添加环境检查脚本 `scripts/verify-environment.mjs`。
- 编写 README 和 DSH 技术路线文档，明确只先开发员工管理 Agent，并采用“Mock 成熟后替换正式数据层”的路线。
- 直接使用临时 `npx` 获取 DSH 时出现长时间无输出，已停止该进程，改用项目内精确版本依赖，保证安装和调用方式可复现。
- pnpm 首次安装触发依赖构建脚本保护；已将 DSH 依赖链中需要安装脚本的 5 个精确包显式加入 `allowBuilds`，不采用全局或通配放行。
- 建立严格的 TypeScript 根配置，锁定 TypeScript `7.0.2` 与 Node.js 22 类型定义，并增加 `build`、`typecheck` 命令，为后续 workspace 业务包提供统一基线。

## 2026-08-14 17:17:01 +0800

- 完成依赖安装并生成 `pnpm-lock.yaml`；pnpm 供应链策略检查通过，DSH 所需的原生终端与子进程依赖构建成功。
- 执行 `build` 和 `typecheck`，严格 TypeScript 检查全部通过。
- 执行环境验证，确认本机 Node.js `22.22.2`、项目 DSH `0.1.0-rc.6`、pnpm `11.7.0` 均符合锁定要求。
- 验证 DSH CLI 帮助和 web profile 配置可以正常加载。
- 实际启动 DSH Web，并确认 `http://127.0.0.1:3080/` 返回 HTTP 200；验证完成后已正常停止测试进程。
- 检查项目文件，未发现真实 DeepSeek API Key；模型请求暂不测试，待开发者在本地 `.env` 中配置密钥后进行。

## 2026-08-14 17:30:00 +0800

- 完成 Phase 1 员工管理 Agent 产品与技术规格，定义首批场景、用户角色、权限矩阵、工具契约、统一响应结构、变更确认流程和 Phase 2 验收标准。
- 新建 workspace 包 `@hegongzuo/employee-domain`，提供员工状态、用工类型、员工记录和数据集的 TypeScript 类型。
- 新建员工数据集 JSON Schema，约束字段、枚举、ID、工号、经理关系和数据来源标记。
- 创建 10 名完全虚构的员工测试数据，覆盖 6 个部门、在职/试用/休假状态和多级汇报关系；邮箱使用 `example.com`，电话使用明显无效的测试号码。
- 增加 `validate:data` 命令和数据校验脚本，检查记录数量、必填字段、唯一性、日期、枚举、经理引用和循环汇报。
- 更新根 `build`、`typecheck` 命令，使其同时检查所有 `packages/*` workspace 包。
- 首次严格类型检查发现校验脚本存在 3 处动态 JSON 类型边界问题，已增加明确类型声明；随后发现脚本子进程无法直接解析全局 `pnpm`，已统一改为通过 Corepack 调用。
- 重新执行数据校验、构建和类型检查，全部通过。
- 更新 README 和技术路线，将当前进度调整为 Phase 0、Phase 1 已完成，下一步进入 Phase 2 的 DSH 插件接入。

## 2026-08-14 17:39:34 +0800

- 核对本地 DSH `0.1.0-rc.6` 的插件、工具注册、输出 Schema、系统提示和 profile bundle API，确认使用 Cordis bundle 扩展，不修改 DSH 内核。
- 新建 `@hegongzuo/employee-agent` workspace 包，声明 `dsh.bundle` 和 `cordis.patch.yml`，使插件能通过 DSH 官方 profile 机制加载。
- 实现只读员工仓储，支持关键词、部门、岗位、状态、地点和分页查询，以及按 ID/工号查询与部门成员查询。
- 注册 `employee_search`、`employee_get`、`organization_list_members` 三个模型兼容的只读 DSH 工具，并为每个工具声明输入、输出 Schema 和并发安全属性。
- 增加员工管理系统提示，要求 Agent 明确说明当前数据为虚构测试数据，并禁止声称执行了入职、调岗、离职或员工信息修改。
- 插件构建时从唯一的员工领域 fixture 复制运行数据，避免手工维护两份测试数据；运行时只接受带有 `synthetic-non-personal` 标记的数据集。
- 增加 6 项自动化测试，覆盖数据加载、技术部查询、工号查询、经理解析、空部门结果、工具定义和 DSH 插件入口注册。
- 首次类型检查发现插件缺少 Node 类型和可选参数边界问题，已在保持严格类型规则的前提下修复，未关闭 `exactOptionalPropertyTypes`。
- 通过 Corepack 启用 pnpm shim，并将本地员工插件安装到 `~/.dsh/profiles/web`；profile manifest 已包含 `@hegongzuo/employee-agent` bundle。
- 因用户正在运行的 DSH 占用 3080 端口，保留该进程不做中断；改在 3081 启动独立验证实例，DSH Web 成功加载插件并返回 HTTP 200，验证后已停止 3081 实例。
- 更新 README、员工 Agent 规格和技术路线，补充插件安装、运行时工具名、网页测试方式及当前 Phase 2 进度。
- 最终执行测试数据校验、构建、类型检查和 6 项自动化测试，全部通过。

## 2026-08-17 10:31:35 +0800

- 根据新的产品优先级，暂停扩展 Agent 功能，开始交付“和工作”线上办公协作平台的第一版静态视觉原型。
- 新建 `apps/web` React 应用，使用成熟企业科技蓝设计，完成品牌入口页、侧边导航、顶部工具栏、响应式布局和通用视觉系统。
- 建立面向全体员工的分角色工作台，可预览普通员工、团队负责人、HR 管理员、管理层和超级管理员五种界面状态。
- 将系统管理员定义为企业所有者和平台最高权限角色，增加仅超级管理员可见的企业管理入口与静态管理页。
- 完成工作台、消息、项目、任务、文档、日程、员工、Agent 中心和企业管理的页面样式；除员工管理 Agent 外，这些模块仅作为静态界面预览。
- 将平台侧 Agent 入口品牌定为“和助手”，增加全局助手抽屉、员工页入口和 Agent 中心；实际员工查询通过链接进入已安装 bundle 的 DSH Web。
- 增加 `platform:dev` 和 `platform:preview` 命令，并将 `apps/*` 纳入 pnpm workspace、根构建和类型检查流程。
- 新增平台环境变量示例 `VITE_DSH_WEB_URL`，默认指向本地 DSH Web 的 `http://127.0.0.1:3080`。
- 处理 DSH `0.1.0-rc.6` 依赖链的 React 主版本分叉，在 workspace 将 React 和 React DOM 统一锁定为兼容的 `18.3.1`，`pnpm peers check` 无遗留依赖问题。
- 更新 README 和技术路线，新增平台视觉原型说明，明确已交付界面、Agent 连接方式和未实现功能的边界。
- 启动平台生产构建预览并验证首页、JavaScript 和 CSS 均返回 HTTP 200；最终环境检查、10 名虚构员工数据校验、严格类型检查、平台生产构建、6 项 Agent 测试、peer dependency 检查和文件格式检查全部通过。

## 2026-08-17 11:04:41 +0800

- 根据“一步一步开发”的新要求大幅收缩平台范围，删除消息、项目、任务、文档、日历、员工列表和企业管理等未开发模块的页面与导航。
- 删除原来集中在单个 `App.tsx` 中的页面与角色逻辑，建立 `app`、`components`、`config`、`modules` 和 `styles` 分层目录。
- 新建唯一模块注册表 `module-registry.ts`，统一管理功能 ID、导航名称、描述、图标、组件和允许角色；导航和访问过滤不再重复维护。
- 将概览、员工管理 Agent 和开发控制台拆分为三个独立功能目录，每个模块拥有自己的 React 组件与 CSS，可独立增加、删除或修改。
- 将原来的全局角色模型精简为“老板”和“开发者”；老板代表企业所有者和最高业务权限，开发控制台仅开发者可见。
- 保留前端身份切换作为内部评审工具，并在界面与文档中明确说明它不是正式账号认证；HR、管理者和普通员工尚未加入当前代码。
- 重写员工管理 Agent 模块，将 DSH Web 直接嵌入“和工作”内容区，删除跳转到外部页面的入口，增加内嵌页重新加载与平台内专注模式。
- 将原来集中的 3,063 行平台 CSS 替换为设计变量、全局基础、平台外壳和三个模块局部样式，降低后续修改的相互影响。
- 更新 README、技术路线和平台原型说明，补充模块的增加、删除、修改方式以及 DSH 内嵌运行结构。
- 验证当前运行中的和工作开发服务与 DSH Web 均返回 HTTP 200，DSH 未返回禁止内嵌的响应头；全项目环境校验、数据校验、类型检查、生产构建、6 项 Agent 测试、peer dependency 检查和格式检查全部通过。

## 2026-08-17 11:13:06 +0800

- 删除登录页的身份选择和登录后的角色切换，改为标准账号、密码登录表单。
- 新建独立账号边界 `accounts.ts`，由账号记录自动返回老板或开发者角色，并将当前用户姓名、账号和角色传入平台权限过滤。
- 新增 `boss / demo123` 和 `developer / demo123` 两个本地测试账号；老板登录后默认进入管理驾驶舱，开发者默认进入概览。
- 在代码与界面中标记当前登录仅用于本地开发，正式上线前必须将前端账号校验替换为服务端认证与 API 权限校验。
- 新增仅老板可见的“管理驾驶舱”模块，并通过统一模块注册表接入导航和角色访问过滤。
- 完成员工、项目、销售、财务和审批五类核心指标卡，以及销售/回款趋势图、分级异常提醒、子系统健康度和 AI 经营分析区域。
- 将驾驶舱所有指标、趋势数组、异常、系统状态和 AI 建议集中到独立 `cockpit-data.ts`，与页面布局和局部样式分离。
- 当前驾驶舱数据均为测试用虚构经营数据，AI 分析为 Mock 文本，未连接真实 HR、项目、CRM、财务或审批系统，也未调用模型。
- 为平台新增 4 项账号测试，覆盖老板角色映射、开发者角色映射、账号归一化和错误密码拒绝。
- 更新 README、平台模块化说明和技术路线，新增管理驾驶舱专项文档，记录数据边界和正式子系统接入顺序。
- 最终检查确认前端代码中已无身份选择器和角色切换器，运行中的平台和 DSH 均返回 HTTP 200；环境校验、数据校验、严格类型检查、生产构建、4 项账号测试、6 项 Agent 测试和依赖检查全部通过。

## 2026-08-17 11:27:34 +0800

- 定位员工管理 Agent 在不同平台账号间共享会话记录的原因：老板和开发者同时嵌入默认 `3080` DSH Web，共享同一浏览器 origin 与默认 `~/.dsh/sessions` 服务端会话目录。
- 将平台模块属性从仅接收角色扩展为接收完整登录用户，员工 Agent 现在使用账号 ID 解析专属运行时。
- 新增统一账号 Agent 运行时定义 `config/account-agent-runtimes.json`：`boss` 分配 3180 端口，`developer` 分配 3181 端口。
- 为两个账号创建 `.runtime/dsh/boss` 和 `.runtime/dsh/developer` 两个独立 DSH_HOME，并分别初始化 web profile、安装 `@hegongzuo/employee-agent` bundle。
- 新增 `dsh:accounts:setup` 命令用于幂等配置全部账号运行空间，新增 `dsh:accounts` 命令用于同时启动所有专属 DSH Web。
- 员工 Agent iframe 改为按登录账号使用独立 URL，iframe key 包含账号 ID；登出时组件完全卸载，下一账号不会复用前一账号的 iframe。
- 删除共享 DSH URL fallback；任何未配置专属运行时的账号都会看到隔离保护提示，不会被自动导向共享会话。
- 新增 3 项隔离回归测试，检查账号的浏览器 origin 不同、账号/端口唯一以及未配置账号无共享 fallback。
- 新增 `verify:agent-isolation` 服务端验证命令，已确认两个 DSH_HOME 独立、两个端口独立、插件已分别安装，且 3180 和 3181 均返回 HTTP 200。
- 原默认 `~/.dsh` 中的共享记录未删除，但平台不再访问 3080；将 `.runtime/` 加入 Git 忽略，避免提交本地账号会话和配置。
- 更新 README、平台说明和环境变量模板，新增 Agent 账号隔离专项文档。
- 首次全量测试发现 Node.js 直接运行 TypeScript 测试时要求 JSON import attribute，已增加 `with { type: 'json' }` 并使用显式 `.ts` 扩展名，同时保持 Vite 构建与 Node.js 测试兼容。
- 最终环境校验、测试数据校验、严格类型检查、生产构建、7 项账号/隔离测试、6 项员工 Agent 测试、peer dependency 检查与运行时隔离检查全部通过。

## 2026-08-17 11:50:54 +0800

- 删除员工管理 Agent 模块中的 DSH Web iframe，不再向老板或开发者展示 DeepSeek/DSH Logo、导航、工作区选择和产品声明。
- 新建“和工作”原生员工 Agent 操作界面，包含最近会话、新建对话、历史消息、推荐问题、消息输入、连接状态、运行状态、工具调用状态和错误提示。
- 新建账号 DSH API 客户端，通过平台同源代理调用 DSH 的 Workspace、Session、History 和 Prompt HTTP API，并使用 WebSocket 接收会话与运行状态事件。
- Vite 开发与预览服务新增按账号划分的 HTTP/WebSocket 代理：`/dsh/boss` 转发到 3180，`/dsh/developer` 转发到 3181；浏览器不再直接加载 DSH Web origin。
- 新建 DSH 原始事件到“和工作”消息模型的转换层，支持用户消息、流式助手文本、最终回答、工具调用/结果以及轮次错误的原生展示。
- 将账号运行时定义扩展为同时声明唯一 API 路径和唯一 Workspace；老板与开发者分别使用 `.runtime/workspaces/boss/employee-agent` 和 `.runtime/workspaces/developer/employee-agent`。
- 更新账号运行时 setup/start 脚本：自动创建 Workspace，以该目录作为 DSH 进程 cwd，并通过环境变量将账号工作区传入员工 Agent 插件。
- 员工 Agent 插件增加 DSH Workspace Registry 依赖，在运行时启动时自动注册后台分配的工作区；用户不再输入或选择本地文件路径。
- 扩展隔离验证，新增 DSH_HOME、端口、API 路径、Workspace 唯一性和 Workspace 实际注册检查；两个账号的服务与工作区均验证通过。
- 新增 2 项对话事件转换测试，并扩展账号配置和员工插件测试；平台共 9 项测试、员工 Agent 共 6 项测试全部通过。
- 新增 `THIRD_PARTY_NOTICES.md`，保留 DSH 的 MIT License 归属和发布义务，同时明确第三方技术名称不作为“和工作”的用户界面品牌。
- 更新 README、平台说明、账号隔离说明和技术路线，记录原生 UI、自动 Workspace、平台代理及正式部署的认证边界。
- 重新安装并启动两个账号专属 DSH 运行时和“和工作”开发服务；通过平台代理验证 Workspace API、Session API、History API 与 WebSocket `session/subscribed` 事件均正常工作。

## 2026-08-17 13:11:05 +0800

- 修复浏览器调用员工 Agent 时 `transport failure for /api/workspace.list: HTTP 403`：根因是浏览器的平台代理 Origin 被原样转发，未通过 DSH 运行时的同源安全校验。
- 为每个账号的 Vite HTTP/WebSocket 代理显式重写 Origin，使代理到 3180/3181 的请求使用对应 DSH 运行时 Origin，同时保留浏览器只访问“和工作”同源路径的架构。
- 使用携带真实浏览器 Origin 的 Workspace API 请求验证修复，返回 `ok: true`；使用 WebSocket Upgrade 请求验证 `/dsh/boss/api/events.mux` 返回 HTTP 101。

## 2026-08-17 13:17:39 +0800

- 新增根目录 `AGENTS.md` 项目执行约束，明确只执行用户当前命令要求及不可缺少的最小操作，禁止自行扩展为构建、测试、验证、重启、部署、重构或其他修改。
- 明确命令存在歧义或确需扩大范围时，必须先停止并取得用户许可。
- 保留开发日志更新要求，并明确任何日志、文档和输出均不得记录或回显 API Key、密码等秘密值。

## 2026-08-17 13:25:41 +0800

- 在员工管理 Agent 标题区新增“维护员工数据”按钮，点击后进入“全体员工信息”页面，可查看当前全部员工档案。
- 在全体员工页面新增“进入维护”按钮，进入后提供员工搜索、新增、详情查看、编辑和删除操作。
- 新增员工编辑侧栏，覆盖编号、姓名、邮箱、电话、部门、岗位、直属经理、用工类型、状态、入职日期和工作地点字段，并校验必填项、员工编号及邮箱唯一性。
- 删除员工时同步清空其下属的直属经理引用，避免保留无效汇报关系。
- 当前维护结果保存于浏览器本地存储，并在界面标注为“本地测试数据”；未新增后端数据库或修改 DSH Agent 的只读数据源。

## 2026-08-17 13:28:02 +0800

- 将员工数据维护从 Agent 内部入口扩展为平台独立模块，在侧边导航新增“员工数据”，老板和开发者均可直接进入。
- 新增轻量模块适配组件，通过统一模块注册表接入现有导航与权限过滤逻辑。
- 侧边导航入口与员工管理 Agent 内入口复用同一个员工列表、维护界面和浏览器本地数据源，没有复制业务逻辑或产生两份员工数据。

## 2026-08-17 13:55:42 +0800

- 新增 `apps/api` TypeScript 服务，使用 PostgreSQL 提供员工列表、员工详情、新增、更新和删除 REST API。
- 新增 PostgreSQL 员工表迁移，包含员工编号与邮箱唯一约束、用工类型与状态约束、直属经理外键、自我汇报限制及查询索引。
- 新增数据库迁移与测试数据初始化命令；初始化操作幂等保留已有员工，不覆盖数据库中已经修改的记录。
- 将员工数据维护界面从浏览器 `localStorage` 切换为真实员工 API；数据库不可用时明确显示错误，不再回退到本地假数据。
- 新增前端 `/api/employees` 同源代理，并调整 `platform:dev`，使平台开发命令同时启动 Web 与员工 API 服务。
- 在 Git 忽略的 `.runtime/postgres` 初始化并启动本地 PostgreSQL，仅监听 `127.0.0.1:5432`；连接配置写入 Git 忽略的 `.env`，未记录任何秘密值。
- 完成员工表迁移并写入 10 条虚拟员工测试数据；真实新增、读取、更新、删除检查全部通过，临时验证记录已删除，最终数据库保持 10 条记录。
- 通过“和工作”开发代理读取员工 API 成功，返回 HTTP 200 和 10 条数据库员工记录。
- DSH 员工管理 Agent 本轮未改动，仍使用其现有只读数据源；本次真实 CRUD 范围为“和工作”员工数据维护模块。

## 2026-08-17 14:09:06 +0800

- 按新的员工数据要求，从共享员工领域模型、测试数据、DSH 查询结构、PostgreSQL API 和平台维护界面中删除“员工编号”和“部门编码”。
- 新增 PostgreSQL `002` 迁移并执行，真实删除 `employees.employee_no` 与 `employees.department_code` 两列，保留其余 11 条员工记录。
- 将 `work_email` 从必填字段改为可空字段；数据库解除非空约束，API 接受 `null` 或空值，维护表单改为“工作邮箱（选填）”。
- 更新数据库初始化脚本与员工测试数据 Schema，确保全新数据库也直接使用新的字段结构。
- 更新员工 Agent 的现有只读工具结构：关键词不再匹配员工编号或部门编码，员工详情仅按员工 ID 查询，部门成员仅按部门名称查询。
- 使用空邮箱完成真实新增与删除验证，接口响应确认不再包含已删除字段；临时记录已清除，数据库最终保持 11 条员工数据。

## 2026-08-17 14:25:38 +0800

- 将员工预览“小眼睛”按钮从“进入维护”模式移至全体员工信息主界面；维护模式仅保留编辑和删除操作。
- 小眼睛改为打开员工简历侧栏，集中显示员工职责、简历文件信息和简历内容；PDF 可在平台侧栏内预览，DOC/DOCX 提供查看或下载入口。
- 新增员工与编辑员工表单增加“员工职责”多行文本框，以及 PDF、DOC、DOCX 简历上传控件，单文件限制为 5 MB。
- 新增 PostgreSQL `003` 迁移，增加职责、简历文件名、简历 MIME 类型和简历二进制列；简历文件真实保存为 PostgreSQL `bytea` 数据。
- 新增员工简历读取接口，返回原始文件内容及正确的内容类型、文件名和禁止缓存响应头。
- 扩展共享员工领域模型、测试数据 Schema、测试 fixture 和员工 Agent 输出结构，使其包含职责与简历元数据，但不向 Agent 返回简历二进制内容。
- 使用临时 PDF 完成真实职责写入、简历上传、元数据读取和文件内容读取检查，结果全部通过；临时员工已删除，数据库保持 11 条员工记录。

## 2026-08-17 14:30:45 +0800

- 修复员工简历预览侧栏中“编辑员工”按钮点击后缺少明确界面切换的问题。
- 点击“编辑员工”现在会进入员工数据维护模式，重新载入当前员工草稿，并强制将简历预览侧栏切换为可编辑表单。
- 开发服务热更新完成且未报告该组件编译错误。
- 只读检查确认 PostgreSQL 当前有 1 份真实简历二进制数据，存储大小为 1,015,354 字节；查询未读取或输出简历内容。

## 2026-08-17 14:41:14 +0800

- 修复员工维护页面新增员工后，员工管理 Agent 仍固定返回 10 名测试员工的数据不同步问题。
- 新增 Agent 专用 PostgreSQL 只读仓储；每次 `employee_search`、`employee_get` 或 `organization_list_members` 工具调用都会重新读取当前员工表快照，不缓存固定 JSON。
- 将员工工具抽象为统一只读数据源接口，保留内存仓储用于测试，同时让正式 DSH 插件使用 PostgreSQL 仓储。
- 更新 Agent 系统提示与工具描述，移除“固定虚构测试数据”声明，明确员工数量和名单必须先调用工具获取最新数据库数据，且工具仍禁止写入、修改或删除。
- 为 Agent 插件增加 PostgreSQL 依赖、连接启动检查与 Cordis 生命周期连接池清理；老板和开发者运行时共享公司员工数据，但继续保持各自会话和工作区隔离。
- 员工领域、平台、API 和 Agent 全量构建成功；重新安装插件并重启 3180/3181 两个账号专属 DSH 运行时。
- 直接执行新版 `employee_search` 返回 11 条记录；进一步通过老板 DSH 运行时发起真实 Agent 提问，确认调用了 `employee_search` 且回答包含当前 11 名员工。

## 2026-08-17 15:09:02 +0800

- 在员工管理 Agent 的“最近对话”列表中为每条对话增加独立删除按钮，支持鼠标悬停、键盘聚焦、删除中状态和正在处理时禁用。
- 删除前显示不可恢复确认；删除当前对话后自动切换到剩余的最新对话，无剩余对话时回到空白欢迎界面。
- 员工 Agent DSH 插件新增账号运行时内的会话永久删除路由，精确校验会话 ID、所属工作区和 JSONL 存储路径，禁止跨账号或跨工作区删除。
- 实现 DSH 空闲 Agent 句柄跟踪：运行中的对话拒绝删除，空闲对话先通过 DSH 原生句柄有序停止、刷新并卸载，再执行持久化清理，避免会话文件与内存状态竞态。
- 后台删除同步清理工作区会话引用、会话标题等投影缓存，以及完整会话日志目录；不再使用仅隐藏文件的 DSH 归档接口。
- 在开发者账号中新建并删除一条专用空白临时对话，确认删除后会话目录、工作区引用、投影缓存和会话列表均不再包含该会话。

## 2026-08-17 15:16:33 +0800

- 将“后续开发必须模块化”写入根目录 `AGENTS.md` 项目执行约束，作为持续适用规则。
- 明确平台功能的独立目录和统一模块注册要求，禁止在导航、入口和权限中重复硬编码。
- 明确界面、状态、数据访问、业务规则、Agent 能力和第三方框架适配的职责边界，要求通过接口组合。
- 明确跨模块共享边界、禁止依赖其他模块内部实现，并要求模块增删替换不得破坏其他功能。

## 2026-08-17 15:35:58 +0800

- 修复 `platform:dev` 只启动 API 与 Web、未启动账号 DSH Agent 运行时的架构缺口，该缺口会导致平台代理 `/api/workspace.list` 时无法连接 3180/3181 而返回 HTTP 502。
- 更新 `scripts/run-platform-development.mjs`：平台启动时自动执行账号 Agent 初始化、启动两个专属 DSH 运行时，并等待 boss、developer 的 3180/3181 端口可访问后才启动平台 API 与 Web。
- 新启动器将 Agent 运行时作为平台子进程统一管理；任一 Agent 运行时退出时，平台启动器会同步停止 API 和 Web，避免留下持续返回 502 的孤立平台页面。
- 完成启动验证：4173 平台、3180 boss Agent、3181 developer Agent 均返回 HTTP 200；通过平台代理调用 boss `workspace.list` 返回成功。

## 2026-08-17 15:46:22 +0800

- 处理重复执行 `platform:dev` 时 4173、4174 端口被此前平台实例占用而导致 Vite 和 API 启动失败的问题。
- 更新平台启动器，在构建和启动任何子服务前预检 4173、4174、3180、3181 端口；如有占用，直接指出端口及“先在旧终端按 Ctrl+C 停止”的处理方式，不再进入 Vite/API 的异常堆栈。
- 停止本次诊断前早先启动的旧平台实例，已确认 4173、4174、3180、3181 均不再被占用。
- 执行完整类型检查通过。

## 2026-08-17 15:55:03 +0800

- 在员工数据维护的“员工简历（选填）”区域增加“删除简历”按钮；编辑已有员工时确认保存后会清除 PostgreSQL 中该员工的简历文件名、类型和二进制文件数据。
- 删除尚未保存的新选中文件时仅移除待上传文件；删除后仍可重新选择同一文件上传。

## 2026-08-17 16:03:56 +0800

- 对平台前端进行去 AI 风文案调整：移除“老板专属视图”“最高权限”“和助手 · 企业老板”“随时可以提问”等身份宣传式表达。
- 移除员工数据页维护入口旁的“PostgreSQL 数据库”标识，并将数据库实现细节改为中性的系统数据描述。
- 将导航、概览、登录页和员工查询界面的“Agent”“DSH”“虚拟测试数据”等面向使用者的技术或 AI 化表述改为员工查询、员工信息和办公平台语言；不改变员工查询能力及后台集成。
- 将驾驶舱中的“AI 经营分析”改为“经营提示”，保留现有数据展示与参考信息功能。

## 2026-08-17 16:08:12 +0800

- 移除员工查询侧栏的账号“专属空间”说明，以及空闲状态下的“可以开始查询”提示；查询进行中仍显示“正在查询”。

## 2026-08-17 16:09:18 +0800

- 将前端去 AI 风要求写入项目执行约束：统一中性办公语言，隐藏实现与营销术语，移除拟人化、身份宣传和冗余状态文案，并避免 AI 暗示性视觉元素。

## 2026-08-18 14:04:48 +0800

- 将 `data/王叔和在职.xlsx`（82 名在职员工、36 列人事档案）作为正式员工数据源，新增 `data/` 到 Git 忽略，禁止真实身份证、银行卡、住址进入仓库。
- 新增 PostgreSQL `004` 迁移：employees 表增加 25 个个人档案列（所属公司、性别、身份证、出生日期、学历、紧急联系人、银行卡、档案编号、备注、转正与合同日期等），`work_location` 改为可空，身份证增加部分唯一索引；银行卡刻意不加唯一约束（存在两人共用一张卡）。
- 新增 `apps/api/scripts/import-employees.mjs`：解析 Excel（公式取缓存结果、超链接取显示文本、日期按本地时区取值）、`/` 转 NULL、用工类型映射（合同工→full_time、实习协议→intern）、状态按实际转正日期推导；默认 dry-run 只输出问题清单 `data/import-issues.md`（51 项、9 类，含试用期未填写 22 人、转正日期不一致 13 人等，数据均原样导入不自动修改），`--apply` 在单事务中清空 employees 表后全量导入并校准序列。
- `db:seed` 改为执行正式数据导入（破坏性：清空后导入），删除原 `scripts/seed.mjs`；10 名虚构 mock fixture 仅供 Agent 测试与 `validate:data`。
- 领域包 `EmployeeRecord` 追加 25 个可选可空字段，新增 `sensitiveEmployeeFields` 敏感字段单一事实源；API 输入校验、仓储列映射与 INSERT/UPDATE 同步扩展，工作邮箱/身份证唯一冲突按约束返回不同提示，搜索覆盖所属公司、二级部门与个人邮箱。
- Agent 输出白名单未增加任何敏感字段；`postgres-repository` 对可空的 `work_location` 做空串容错，模型无法通过工具获得身份证、银行卡或住址。
- 前端员工数据模块升级：列表新增“合同到期”列（剩余天数，60 天内警示）；详情抽屉改为六组档案卡（任职/身份/联系/教育/财务/备注），年龄、工龄、合同剩余天数由前端动态计算；新建 `MaskedText` 组件，身份证、银行卡、住址默认脱敏（如 `3411**********5817`）点击眼睛图标显示原文；编辑表单补齐全部档案字段（选填）并支持身份证重复校验。
- 新增 8 项前端纯函数测试（脱敏规则、合同剩余天数、年龄、工龄）。
- 更新员工 Agent 规格（新增 2.1 敏感数据政策，撤销“不存储身份证/银行卡/住址”旧声明，薪酬与健康信息仍不存储）、README 员工数据说明、第三方软件声明（ExcelJS）与开发日志。
- 已知遗留：`apps/web/test/accounts.test.mjs` 中老板显示名断言仍为“企业老板”，与去 AI 风后 `accounts.ts` 的“管理员”不一致，本次未改动该测试。

## 2026-08-18 14:30:00 +0800

- 修复 API 与 Agent 仓储中 pg date 列经 `toISOString` 输出导致东八区日期偏移一天的既有问题，统一改为本地 getter 格式化；已用林韦康（入职 2022-11-10）、外籍员工 KADALIPURA（入职 2026-07-27）与出生日期、合同到期日期逐项核对，API 返回与 Excel 一致。
- 全量验证：根构建、严格类型检查、数据校验、6 项 Agent 测试、17 项平台测试（16 项通过，1 项为既有 accounts 显示名断言不一致）、8 项新增脱敏/派生计算测试全部通过；`data/` 已被 Git 忽略。

## 2026-08-18 14:19:39 +0800

- 按老板反馈调整员工列表：联系方式列改为手机号在前、邮箱在后；删除直属经理列；“入职 / 地点”列改为“入职时间”，只显示入职日期。
- 调整员工状态推导规则并重新导入 82 人：只有试用期时长有值且尚无实际转正日期的员工才标记“试用期”；试用时长与转正日期均无数据的默认按在职处理。当前 82 人全部为在职状态。
- 员工数据页新增“导出”按钮：导出为 Excel（exceljs 按需动态加载，字段与原 Excel 36 列一致，年龄/工龄/合同剩余天数动态计算）与导出为 PDF（打开横向 A4 打印窗口，通过系统打印对话框另存为 PDF）。导出内容包含身份证、银行卡、住址等完整档案字段，仅限管理者在员工数据页使用。

## 2026-08-18 14:24:41 +0800

- 员工列表排序由姓名改为按入职时间升序（同日按员工 ID），与原始 Excel 顺序一致；搜索过滤时保持同一排序，导出文件同样按此顺序。

## 2026-08-18 14:31:00 +0800

- 员工数据页新增排序功能：可按入职时间、姓名、部门、合同到期四种条件排序，支持升/降序切换（按钮箭头指示）；默认入职时间升序。无合同到期日期的员工在“合同到期”排序下始终排在最后。排序在前端完成，导出文件跟随当前排序。
- 排序比较逻辑抽取到独立模块 `apps/web/src/modules/employee-agent/employee-sort.ts`，新增 3 项排序测试。

## 2026-08-18 14:42:00 +0800

- 同步更新员工 Agent 运行时：重新构建 `@hegongzuo/employee-agent`、通过 `dsh:accounts:setup` 重装 boss/developer 两个账号的插件 bundle，并重启平台与全部账号专属 DSH 运行时（3180/3181）与平台服务（4173/4174），全部返回 HTTP 200。
- 验证 Agent 数据层已同步正式数据库：`employee_search` 总数为 82 人，入职日期无时区偏移（如 KADALIPURA 2026-07-27），`work_location` 空值容错生效，输出字段白名单不含身份证、银行卡、居住住址、身份证地址四类敏感字段；`organization_list_members`（品牌中心）返回 25 人。

## 2026-08-18 14:55:00 +0800

- 员工查询 Agent 的字段能力与正式数据库同步：工具输出白名单新增 17 个非敏感档案字段（所属公司、二级部门、性别、出生日期、学历、专业、毕业学校、毕业时间、婚否、育否、籍贯、试用期与转正日期、合同到期日期、档案编号、备注），工具输出 Schema、仓储查询与搜索匹配（所属公司/二级部门/学历/学校）同步扩展；输出字段从 16 个增至 33 个。
- 受限字段边界明确写入系统提示：身份证、银行卡、居住住址、身份证地址、紧急联系人及电话、个人邮箱不进入工具输出，模型无法查询。
- 重新构建插件、重装 boss/developer 运行时并重启平台，全部服务 HTTP 200；验证“完素”搜索命中张发恒、受限字段零泄漏、新字段值正确。

## 2026-08-18 15:20:00 +0800

- 员工查询 Agent 新增两个只读工具：
  - `employee_stats`：员工总体分布统计（总人数 + 按所属公司、部门、学历、性别、用工类型、状态、年龄段、司龄段分组计数，同人数按中文拼音排序）。
  - `contract_alerts`：合同与转正预警（合同在 N 天内到期的员工、已过期未续签员工、预计转正日期临近但尚未实际转正员工；窗口默认 60/30 天可调，按剩余天数升序）。
- 两个工具复用现有快照仓储，无新增查询路径；输出不包含任何敏感字段。
- 更新系统提示说明新工具用途；Agent 测试扩展为 8 项全部通过；重新安装插件并重启 boss/developer 运行时，全部服务 HTTP 200。
- 真实数据验证：统计口径 82 人（工程 58/医药 22/完素 1/子和 1，本科 39、女 42/男 40）；60 天合同预警命中 4 人（郭春霖 2026-08-21 仅剩 3 天、代琛琛、曾维威、张颖）。

## 2026-08-18 15:40:00 +0800

- 员工查询主界面删除 3 个过时快捷问题，替换为四宫格功能介绍（员工查询、组织关系、统计分布、合同与转正预警），文案保持中性办公语言。
- 新增轻量 Markdown 渲染组件 `markdown.tsx`（无第三方依赖）：Agent 回答中的加粗、斜体、行内代码、表格、无序列表、代码块与段落正确渲染，所有文本先做 HTML 转义防止注入；assistant 消息统一改用该组件渲染。
- 新增渲染样式：表格横向滚动、代码块底色、加粗正文等，沿用设计变量。

## 2026-08-18 15:55:00 +0800

- 全链路删除“直属经理”概念：新增 `005` 迁移删除 employees 表 `manager_id` 列（连同外键、约束与索引），数据保持 82 名员工不变。
- 同步清理 API 输入校验/仓储 SQL/服务端路由、员工领域模型、Agent 工具输出 Schema 与投影、前端类型/编辑表单/档案卡/删除逻辑、mock 测试数据与数据校验脚本中的 managerId/managerName；`organization_list_members` 描述中不再承诺汇报关系。
- 更新 Agent 测试（删除经理解析断言），全量构建、严格类型检查、数据校验、8 项 Agent 测试全部通过；平台测试 19/20（1 项既有 accounts 断言问题）。

## 2026-08-18 16:33:28 +0800

- 按《和工作平台设计规范》（玉青白 · 王叔和品牌风格）重构前端视觉：
  - `tokens.css` 重写为玉青白色板：深墨青 `#163F3A`、交互青绿 `#2F8C78`、王叔和玉青 `#4FA38E`、淡玉青 `#EDF7F4`、页面背景 `#F5F8F7`，新增成功/警告/危险状态色（`#16835D`/`#C47A20`/`#C84B4B`），圆角收敛为 8/12/16px，阴影改为浅青绿投影；旧变量名保留为新值别名。
  - 登录页由深藏青渐变改为青灰白浅色品牌区；侧边栏由深色改为白色 + 淡玉青选中态（规范不建议整块深色侧边栏）；概览 Hero 与驾驶舱深色卡片改为深墨青渐变。
  - 品牌标记去掉 Sparkles 图标（规范禁止 AI 暗示元素），保留纯几何图形，渐变换为王叔和玉青系。
  - 驾驶舱指标卡由彩色系收敛为品牌色系；全部模块 CSS 中的硬编码蓝灰色替换为新色板（表格表头、输入边框、focus ring、错误/警告状态色等）。
  - 基础字号按规范提升（辅助信息 11-12px、表格正文 12px+），保持紧凑但不缩小可读性。
- 验证：类型检查、生产构建、平台测试（19/20，1 项既有）通过；运行中平台热更新已生效。

## 2026-08-18 16:50:00 +0800

- 使用《和工作logo.png》作为平台 Logo：图片优化为 256px 版本（68 KB）放入 `apps/web/public/logo.png`，`BrandMark` 组件改为图片引用，删除原 CSS 几何图形 Logo（含渐变方块与旋转线条样式）。
- `index.html` 增加 favicon 引用，浏览器标签页同步显示新 Logo。
- 验证：类型检查、生产构建通过，运行中平台可直接访问 /logo.png（HTTP 200），刷新页面生效。

## 2026-08-18 17:05:00 +0800

- 老板账号信息更新：显示名由“管理员”改为“陶春霖”，角色标识由“老板”改为“CEO”（首席执行官），顶栏与登录提示不再显示账号英文标识；概览问候改为“你好，陶总”，其他账号显示“你好，{姓名}”。
- 同步修复既有 accounts 测试断言（显示名），平台测试 20/20 全部通过；README 账号表更新为陶春霖（CEO）。登录账号 `boss` 仅作为内部标识保留。

## 2026-08-18 17:12:00 +0800

- 开发者账号显示名由“平台开发者”改为“刘奥”。
- 顶栏头像标识由角色固定字（“和”）改为按登录用户姓名首字动态显示（陶春霖→陶、刘奥→刘），不做硬编码。
- 登录提示卡此前已改为显示姓名（陶春霖 / 刘奥），刷新页面后不再出现英文账号标识；README 账号表同步。

## 2026-08-18 17:40:00 +0800

- 账号系统阶段 A（认证闭环）上线：
  - 新增 `006` 迁移：`accounts` 表（登录名、显示名、scrypt 密码哈希、角色 owner/developer、启用状态）与 `sessions` 表（7 天有效期）。
  - 新增账号种子脚本（`db:seed:accounts`，并入 `db:setup`）：初始账号陶春霖（`taochunlinxx`/owner）、刘奥（`liuao01`/developer），初始密码 demo123。
  - 新增认证接口：`POST /api/auth/login`（登录名+密码→会话 token）、`GET /api/auth/me`（恢复登录态）、`POST /api/auth/logout`（销毁会话）；账号不存在与密码错误返回统一提示。
  - 员工数据 API（列表/详情/新增/更新/删除/简历）全部要求 Bearer token，未登录返回 401。
  - 前端：删除本地账号校验（`accounts.ts` 与相关测试），登录页对接认证接口，登录态存 localStorage（7 天自动恢复，401 自动回登录页）；顶栏退出调 logout。
  - 修复迁移全量重跑问题：`001` 中已删除列（manager_id）的建表语句与索引移除，保持迁移链幂等。
  - 平台测试 13/13 通过；认证流程（登录/me/带 token 访问/无 token 401/登出）经 API 实测全部通过。

## 2026-08-18 18:30:00 +0800

- 账号系统阶段 D（Agent 运行时联动）上线：
  - 新增 `dsh:accounts:sync` 脚本：从 accounts 表读取启用账号，按创建顺序从 3180 起分配端口，生成 `.runtime/account-agent-runtimes.json`（Git 忽略）；`dsh:accounts`、`dsh:accounts:setup` 与平台启动器统一先同步再启动。
  - 旧静态配置 `config/account-agent-runtimes.json` 删除；前端运行时解析与 Vite 代理改为读取生成文件。
  - 账号目录自动迁移：boss/developer 的 DSH_HOME 与工作区按端口顺序改名为新登录名（taochunlinxx/liuao01），会话历史随目录保留。
  - 端到端验证：新账号创建后 sync 自动分配 3182 与 `/dsh/<登录名>` 代理路径；账号隔离验证通过（独立 DSH_HOME、工作区、端口）；四服务全部 HTTP 200。
  - 当前模式为全量启动（平台启动时拉起全部启用账号的运行时），新增账号后重启平台生效；懒启动留待部署阶段。

## 2026-08-18 18:55:00 +0800

- 账号规则与密码流程调整：
  - 登录名规则简化为姓名拼音（小写字母开头，可含数字），不再要求年份简写；现有账号迁移为 taochunlin / liuao，DSH 目录与运行时配置同步改名，会话历史保留。
  - 新建账号不再填写初始密码，统一为默认密码 wangshuhe123；开发者“重置密码”改为“一键重置为默认密码”（确认后直接生效）。
  - 新增“修改密码”：任何账号登录后可从顶栏进入修改自己的密码（校验当前密码、新密码至少 6 位、二次确认），修改后旧密码即时失效。
  - 账号管理界面同步：创建表单去掉密码输入、登录名规则提示更新、重置按钮文案与确认流程更新。
  - 全流程实测通过：自行改密码（旧密码失效/新密码登录）、一键重置后默认密码登录、新建账号默认密码登录、新登录名代理路径（/dsh/liuao）与账号隔离验证通过；平台测试 13/13。
  - 修复 sync 脚本迁移源：目录迁移改为对比上一版生成文件（支持登录名再次变更时自动迁移）。
